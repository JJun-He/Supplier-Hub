# 진행 기록

## 주요 결정 변경 이력

| 주제 | 처음 검토한 방식 | 최종 결정과 이유 |
| --- | --- | --- |
| 내부 식별자 | UUID | 하나의 관계형 DB가 식별자를 발급하므로 `BIGINT IDENTITY` 사용 |
| 카탈로그 실행 | 기동 시 한 번 | 장애 복구와 변경 반영을 위해 기동 직후와 주기 실행 |
| 사라진 매핑 | 삭제 | 재등장 시 내부 ID를 유지하도록 비활성화 |
| 빈 전체 목록 | 정상 전체 목록으로 반영 | 기존 활성 데이터가 있으면 장애 응답일 가능성을 고려하여 반영 보류 |
| 최초 동기화 스레드 | `ApplicationReadyEvent` 동기 실행 | 준비 완료 흐름을 막지 않도록 스케줄러 스레드에서 즉시 실행 |
| 객실 타입 조회 | 숙소마다 조회 | Supplier 단위 일괄 조회 후 숙소 ID로 그룹화 |
| Offer 생성과 격리 | public record와 완성된 후보만 보호 | private 생성자와 원본 항목 변환 전체의 후보별 격리 |

## 2026-09-12 - 도메인과 연동 정책 수립

### 수행 내용

- 전체 요구사항과 두 Supplier 계약을 검토했다.
- Java 21과 Spring Boot로 생성된 초기 프로젝트 상태를 확인했다.
- 숙소, 객실 타입, Offer, 가격, 재고의 내부 경계를 정의했다.
- 고객 응답 상태와 Supplier 실패 처리 방식을 정했다.
- 카탈로그 동기화, 식별자 유지, 타임아웃, 요청 분할, 동시 호출 정책을 정했다.
- 단계별 구현 순서와 커밋 계획을 작성했다.

이 단계에서는 제품 코드를 구현하지 않았다. 코드를 작성하기 전에 변경 비용이 큰 판단을 먼저 명확히 하는 것이 목적이었다.

### 결정과 근거

- 공통 식별자가 없고 추정 병합의 위험이 크므로 Supplier가 다른 숙소는 별도로 관리한다.
- 가격, 재고, 조식 조건이 붙는 단위이므로 객실 타입의 판매 조건을 Offer로 정의한다.
- 내부 처리는 Offer 단위로 유지하면서 고객 응답은 숙소와 객실 타입별로 그룹화한다.
- 현재 하나의 관계형 DB만 사용하므로 내부 ID는 DB 생성 숫자를 사용한다.
- 가격은 세금 포함 숙박 전체 총액으로 통일하고 Supplier가 제공한 날짜별 상세만 내부에서 보존한다.
- 검색 응답 크기와 Supplier 간 일관성을 위해 날짜별 가격은 상품 상세 확장으로 미룬다.
- 검색 목적을 예약 가능한 상품 조회로 정의하여 품절 Offer를 제외한다.
- 해석 가능한 응답에서 일부 Offer만 잘못됐다면 정상 형제 Offer를 보존한다.
- 사용할 수 있는 부분 결과에는 HTTP 200을, 모든 Supplier 실패에는 HTTP 503을 사용한다.
- 카탈로그는 기동 시와 주기적으로 동기화하고 실패 중에도 기존 매핑을 보존한다.
- 다시 등장한 상품의 내부 ID를 유지하기 위해 비활성 매핑을 삭제하지 않는다.
- 고객 응답 시간과 장애 중 호출량을 고려하여 첫 검색 구현에서는 재시도하지 않는다.
- Supplier 원천이 날짜와 인원에 의존하는 요청 방식이므로 검색에 WebSocket, MQ, Kafka를 사용하지 않는다.
- 고객 요청의 50개 분할은 영속 배치 작업이 아니므로 Spring Batch를 사용하지 않는다.
- 무제한 fan-out과 순차 호출 사이에서 Supplier별 제한 병렬성을 선택한다.
- 환율과 결제 정책이 없으므로 원본 통화를 보존하고 변환하지 않는다.
- JPA와의 일관성을 위해 Spring MVC를 사용하고 Supplier HTTP 호출만 `WebClient`로 병렬 처리한다.

### 검토 후 수정하거나 제외한 대안

- 내부 ID로 UUID를 먼저 검토했지만 분산 ID 생성 요구가 없어 숫자 ID로 변경했다.
- 기동 시 한 번만 목록을 동기화하는 방식을 먼저 검토했지만, 장애 복구 후에도 상품이 계속 누락되는 문제가 있어 주기 동기화를 추가했다.
- 평평한 Offer 응답을 검토했다. 내부 모델은 Offer를 유지하되 고객 응답은 숙소와 객실 타입 기준으로 묶기로 했다.
- 검색 응답에서 날짜별 가격을 제공하는 방식을 검토했지만 전체 숙소 검색의 응답 크기를 고려해 상품 상세 확장으로 미뤘다.
- 목록에서 사라진 매핑을 삭제하는 방식은 재등장 시 내부 ID가 바뀌므로 제외했다.
- 하나의 잘못된 항목 때문에 Supplier 응답 전체를 버리는 방식은 독립 검증 가능한 정상 Offer를 잃으므로 제외했다.
- 메시징과 스트리밍 기반 재시도는 메시지 전달과 Supplier 응답 성공이 같은 보장이 아니며 검색 결과의 최신성도 떨어뜨리므로 제외했다.

### AI 활용 기록

AI를 사용해 도메인 경계, API 응답 구조, 내부 식별자, 실패 응답, 동기화 복구, 재시도 방법, 구현 순서를 비교했다.

제안을 그대로 채택하지 않고 현재 범위에 맞는지 대화로 검토했다. UUID 제안은 하나의 DB라는 조건을 적용해 숫자 ID로 수정했다. 기동 시 한 번만 동기화하는 제안은 사용 중 Supplier가 계속 누락되는 문제를 발견해 주기 복구 방식으로 수정했다. WebSocket, MQ, Kafka, Spring Batch는 현재 Supplier 계약과 검색 최신성에 맞지 않아 제외했다.

확정한 근거와 제외한 대안은 `docs/architecture-decisions.md`에 정리했다. 이후 생성되는 코드는 각 결정을 실제로 반영하는지 검토하고, 이해하거나 설명할 수 없는 구현은 유지하지 않는다.

### 다음 작업

1. 기본 빌드를 확인하고 PostgreSQL과 migration을 설정한다.
2. 별도 포트의 최소 Mock Supplier를 구현한다.
3. 카탈로그 매핑과 동기화를 구현한다.
4. `docs/implementation-plan.md` 순서대로 검색 흐름을 완성한다.

## 2026-09-13 - 기반 환경 구성

### 수행 내용

- PostgreSQL 로컬 실행 환경을 Compose로 구성했다.
- Flyway가 스키마 변경을 전달하도록 설정했다.
- Hibernate는 스키마 생성 대신 검증만 수행하도록 설정했다.
- 테스트가 개발자 로컬 DB에 의존하지 않도록 Testcontainers PostgreSQL을 연결했다.
- 애플리케이션 실행 모델을 Spring MVC로 명시했다.

### 의사결정

- 제품 DB와 테스트 DB의 동작 차이를 줄이기 위해 H2 대신 PostgreSQL Testcontainers를 사용했다.
- 로컬 애플리케이션 실행은 상태를 유지할 수 있는 Compose DB를 사용하고, 테스트는 매번 격리되는 임시 DB를 사용한다.
- 스키마 변경 이력을 코드와 함께 추적하기 위해 Hibernate 자동 생성 대신 Flyway를 사용한다.

### 검증 결과

- `./gradlew test`: 성공
- `docker compose up -d --wait`: 성공
- `/actuator/health`: `UP`

## 2026-09-13 - Mock Supplier 구성

- 실제 애플리케이션과 다른 포트에서 실행되는 Mock 모듈을 추가했다.
- 두 Supplier의 목록 및 재고·요금 API를 고정 응답으로 구성했다.
- 정상, HTTP 오류, 본문 오류, 무응답 상황을 재현할 수 있게 했다.
- 후속 연동 개발에서 실제 HTTP 통신과 부분 실패를 검증하기 위해 Mock을 먼저 구현했다.

### 검증

- `./gradlew test`: 성공
- 정상, 장애, 무응답 시나리오 확인

## 2026-09-13 - 카탈로그 매핑 영속화

### 수행 내용

- Supplier 숙소와 객실 타입 코드를 내부 숫자 ID에 연결하는 테이블과 Entity를 추가했다.
- 숙소는 Supplier와 숙소 코드 조합, 객실 타입은 내부 숙소와 객실 타입 코드 조합으로 중복을 막았다.
- 동기화는 비활성 매핑도 찾을 수 있고 검색은 숙소와 객실 타입이 모두 활성인 매핑만 읽도록 Repository 조회를 분리했다.
- 외부 목록 DTO와 동기화 정책은 다음 단계에서 구현할 수 있도록 매핑의 생성, 갱신, 비활성화 동작만 열어 두었다.

### 의사결정

- 내부 식별자는 하나의 관계형 DB가 발급하므로 `BIGINT IDENTITY`를 사용한다.
- 객실 타입 코드는 숙소 안에서만 유일하므로 전체 Supplier 범위의 고유 제약을 두지 않는다.
- 비활성 매핑을 삭제하지 않아 재등장 시 기존 ID를 다시 사용할 수 있게 한다.
- 스키마 생성은 Hibernate가 아니라 Flyway migration이 담당하고 Hibernate는 코드와 스키마의 일치 여부만 검증한다.

### 검증

- 동일 Supplier 숙소 코드 중복 저장 거부
- 서로 다른 Supplier의 동일 숙소 코드 저장 허용
- 동일 숙소의 객실 타입 코드 중복 저장 거부
- 서로 다른 숙소의 동일 객실 타입 코드 저장 허용
- 비활성 매핑의 동기화 조회 허용
- 비활성 숙소 또는 객실 타입의 검색 조회 제외

## 2026-09-13 - Supplier 목록 연동과 동기화

### 수행 내용

- 두 Supplier의 목록 API를 호출하는 `WebClient` 어댑터를 추가했다.
- Supplier별 응답 DTO를 공통 카탈로그 snapshot으로 변환하고, 필수 값과 코드 중복을 반영 전에 검증했다.
- 정상적인 전체 snapshot을 Supplier 단위 트랜잭션으로 반영하도록 구현했다.
- 기존 매핑은 갱신·재활성화하고 목록에서 사라진 매핑은 삭제하지 않고 비활성화했다.
- 애플리케이션 기동 직후와 설정된 주기에 동기화를 실행하도록 구성했다.
- 일시적 네트워크·서버 오류만 제한적으로 재시도하고, 한 Supplier가 실패해도 다른 Supplier 동기화는 계속하도록 했다.

### 의사결정

- HTTP 호출은 트랜잭션 밖에서 완료하고, 검증을 통과한 snapshot의 DB 반영 구간만 트랜잭션으로 묶는다.
- 전체 목록이 아니거나 구조적으로 잘못된 응답은 반영하지 않아 기존 매핑이 잘못 비활성화되는 것을 막는다.
- 기존 활성 숙소 또는 객실 타입이 있는데 0건 목록이 오면 장애 응답일 가능성을 고려하여 전체 반영을 보류한다.
- 호출 한도 오류는 안전한 대기 시간을 알 수 없으므로 현재 단계에서는 즉시 재시도하지 않는다.
- 연결 500ms, 응답 3초, 재시도 2회, 300ms 백오프, 동기화 주기 10분을 초기 설정값으로 두고 환경 변수로 변경할 수 있게 했다.
- 최초 동기화도 스케줄러 전용 스레드에서 실행하여 애플리케이션 준비 완료 흐름을 막지 않게 했다.
- Supplier의 기존 객실 타입을 한 번에 조회하고 숙소별로 그룹화하여 숙소 수에 비례하던 조회를 제거했다.

### 검증

- Supplier별 요청 경로와 API 키 헤더 확인
- Supplier별 성공·실패 응답 변환 확인
- 일시적 오류 재시도와 영구 오류 미재시도 확인
- 동일 목록 재동기화와 이름 변경 후 내부 ID 유지 확인
- 누락 매핑 비활성화와 재등장 시 기존 ID 재활성화 확인
- 한 Supplier 실패가 다른 Supplier 반영을 막지 않는지 확인
- 장애성 빈 숙소·객실 타입 목록이 기존 활성 매핑을 제거하지 않는지 확인
- 예상하지 못한 저장 오류의 스택트레이스 기록 확인
- 최초 동기화가 별도 스케줄러 스레드에서 실행되는지 확인
- `./gradlew test`: 20개 테스트 성공

## 2026-09-13 - 검색 조건과 Offer 도메인

### 수행 내용

- 체크아웃일을 제외하는 검색 기간과 성인·아동 인원 검증을 구현했다.
- 통화의 최소 단위 정수를 `long`으로 보존하는 `Money`를 추가했다.
- 날짜별 세전 금액과 세금을 합산하는 가격과 세금 포함 전체 총액 가격을 하나의 `Price` 의미로 통일했다.
- 요청 숙박일의 누락·중복·범위 초과를 검증하고 일별 재고 최솟값으로 연박 재고를 계산했다.
- 예약 가능한 항목만 `Offer`로 만들고 품절과 잘못된 후보를 서로 다른 결과로 분류했다.
- 원본 항목의 DTO 변환부터 Offer 생성까지 후보별로 격리하여 하나가 잘못되어도 정상 형제 후보를 유지하는 정규화 경계를 추가했다.

### 의사결정

- 공통 가격은 객실 1실의 세금 포함 숙박 전체 금액이며 날짜별 상세는 Supplier가 제공한 경우에만 보존한다.
- 금액 합산 시 `int` 범위를 넘을 수 있으므로 내부 금액은 `long`을 사용하고 오버플로를 허용하지 않는다.
- 통화가 다른 금액은 합산하지 않고 원본 통화를 유지한다.
- 품절은 정상적인 검색 결과이므로 정규화 실패와 별도로 집계한다.
- 성인 최소 1명, 과거 날짜 제한, 최대 숙박 기간은 주어진 제품 정책에 없으므로 추가하지 않았다.

### 검토 후 보완

- 처음에는 이미 생성된 `Price`를 가진 후보만 정규화 구간에서 보호했다. 잘못된 통화나 음수 금액은 후보 생성 전에 실패한다는 문제를 확인하여, 원본 항목을 후보로 바꾸는 함수까지 보호 구간 안에서 실행하도록 변경했다.
- 처음에는 `Offer`를 public 생성자가 있는 record로 만들었다. 검증 팩토리를 우회할 수 있어 private 생성자를 가진 불변 클래스로 변경했다.
- 검증 전 원본처럼 null을 허용하던 `OfferCandidate`를 완전한 도메인 후보로 정의하고 생성 시 필수 값을 검증하도록 변경했다.
- 정규화 예외를 개수로만 집계하던 구현을 수정하여 application 계층에서 Supplier, 항목 순번, 내부 식별자와 스택트레이스를 남기도록 했다.
- Supplier의 `PARTIAL` 상태와 공통 실패 유형은 Offer 도메인이 아니라 후속 통합 검색 서비스가 계산하는 책임임을 문서에 명확히 했다.

### 검증

- 체크아웃일 제외 날짜 범위와 인원 경계값 확인
- 날짜별 세전가와 세금의 숙박 전체 총액 합산 확인
- 세금 포함 전체 총액을 재계산하지 않는지 확인
- 날짜 누락·중복·범위 초과와 음수 재고·금액 거부 확인
- `3, 1, 5`의 연박 재고가 1인지 확인
- 하루라도 재고가 0이면 Offer가 제외되는지 확인
- 잘못된 Offer와 정상 형제 Offer가 격리되는지 확인
- 호출 출처와 후보 Supplier가 다르면 거부하고 두 값을 모두 기록하는지 확인
- `./gradlew test`: 42개 테스트 성공

## 2026-09-13 - Supplier 실시간 검색 어댑터

### 수행 내용

- Supplier A의 `/a/v1/availability`와 Supplier B의 `/b/api/search` 호출 어댑터를 구현했다.
- 검색 조건과 Supplier별 활성 숙소·객실 타입 매핑을 공통 호출 계약으로 정의했다.
- Supplier A의 날짜별 세전가와 세금을 숙박 전체 총액으로 합산하고, Supplier B의 세금 포함 총액은 재계산하지 않고 보존했다.
- Supplier B의 HTTP 200 본문 실패 코드와 Supplier A의 HTTP 오류를 공통 실패 유형으로 변환했다.
- 응답 항목을 DTO에서 `Money`, `Price`, `OfferCandidate`, `Offer`로 바꾸는 전 구간을 후보별로 격리했다.
- 카탈로그와 검색의 WebClient 제한 시간을 분리하고 검색에 연결 500ms, 응답 2초, 호출 전체 3초를 적용했다.

### 의사결정

- 검색 어댑터는 활성 매핑을 직접 조회하지 않고 통합 검색 서비스가 전달한 1~50개 매핑 배치만 사용한다. 다음 단계에서 조회, Supplier별 그룹화, 50개 분할 책임을 한곳에 모으기 위해서다.
- 응답 전체를 해석할 수 없는 오류는 호출 실패로, 특정 항목에 한정된 필드·통화·금액·재고·매핑 오류는 해당 Offer 거부로 처리한다.
- Supplier 전송 DTO는 어댑터 내부 비공개 record로 두어 외부 계약 변경이 검색 도메인으로 퍼지지 않게 했다.
- 실시간 검색은 고객 응답 시간과 장애 중 호출량을 우선하여 재시도하지 않는다.
- 카탈로그와 검색은 같은 Supplier URL과 API 키를 사용하지만 서로 다른 응답 제한 시간이 필요하므로 WebClient 설정을 분리했다.

### 검증

- Supplier별 요청 경로, query parameter, API 키 헤더 확인
- Supplier A 날짜별 가격 합산과 일별 재고 최솟값 확인
- Supplier B 세금 포함 총액 보존과 일별 재고 최솟값 확인
- 알 수 없는 매핑이 정상 형제 Offer를 제거하지 않는지 확인
- 세금 미포함 Supplier B 항목이 Offer 단위로 거부되는지 확인
- Supplier B 본문 오류와 Supplier A HTTP 오류의 공통 실패 변환 확인
- 무응답이 호출 전체 제한 시간 안에 `TIMEOUT`으로 종료되고 재시도되지 않는지 확인
- 50개 초과 호출 묶음과 중복 객실 타입 코드 거부 확인
- `./gradlew test`: 51개 테스트 성공

## 2026-09-13 - 연동 장애 재현과 설정 계약 보완

### 수행 내용

- 실행 중인 Mock Supplier의 A/B 검색 모드를 `normal`, `error`, `no-response`로 변경하는 상태 제어 API를 추가했다.
- 요청 헤더 방식은 개별 테스트 override로 유지하고, 저장된 장애 모드는 실시간 검색 API에만 적용하여 카탈로그 매핑을 안정적으로 확보할 수 있게 했다.
- Mock 검색 응답의 가격과 재고 날짜를 요청한 체크인부터 체크아웃 전날까지 동적으로 생성하도록 변경했다.
- Mock은 요청받은 숙소 코드에 해당하는 항목만 반환하도록 보완했다.
- `CATALOG_UNAVAILABLE`을 공통 실패 enum에 추가하여 설계 문서와 코드의 실패 분류를 일치시켰다.
- Supplier 설정에 Bean Validation을 적용하고 카탈로그 `initialDelay`를 설정 계약에 포함했다.
- 메인 애플리케이션의 Servlet 실행 모델을 설정에 명시하고 기본값과 같은 정적 리소스 설정을 제거했다.

### 의사결정

- 무응답 지연은 검색 제한 시간을 충분히 넘는 30초로 줄이고 Mock에서 virtual thread를 사용한다. 반복 시연으로 플랫폼 스레드가 장시간 점유되는 위험을 줄이기 위해서다.
- 카탈로그 동기화는 백그라운드에서 실행되고 현재 Supplier가 두 개이므로 이번 보완에서는 순차 실행을 유지한다. 대규모 Supplier 추가나 동기화 지연이 관측되면 Supplier 단위 병렬화를 검토한다.
- Supplier당 3,000개를 동시성 4로 조회하면 60묶음, 15 wave가 필요하므로 묶음 p95 1초에서는 5초 예산 안에 전체 완료할 수 없음을 설계 문서에 명시했다.
- 현재 계약은 전체 숙소 조회이므로 임의로 대상을 줄이지 않고, 호출 한도 내 동시성 조정과 bulk 계약 변경을 우선 확장안으로 둔다.
- 다중 인스턴스 잠금, 카탈로그 병렬화, 추가 인덱스, 대량 쓰기, 동기화 상태 테이블, Outbox는 도입 조건과 검증 방법을 설계 문서의 후속 확장 목록에 남기고 근거 없이 선구현하지 않는다.
- 실행 명령을 포함한 `README.md`는 고객 API와 장애 시나리오가 완성되는 9단계에서 실제 동작을 기준으로 작성한다.

### 검증

- 상태 제어 API의 모드 저장과 Supplier별 독립 적용 확인
- 검색 장애 모드에서도 카탈로그 API가 정상 응답하는지 확인
- 요청 헤더가 저장된 모드를 요청 단위로 덮어쓰는지 확인
- Supplier B 장애 모드가 HTTP 200과 `E503` 본문으로 응답하는지 확인
- 임의의 검색 날짜와 요청 숙소 코드에 맞는 동적 응답 생성 확인
- 잘못된 Supplier 또는 모드 변경 요청의 HTTP 400 확인
- 유효한 설정과 잘못된 API 키·타임아웃의 Bean Validation 확인
- `./gradlew test`: 메인·Mock 모듈 합계 59개 테스트 성공
