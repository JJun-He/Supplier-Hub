# 진행 기록

## 주요 결정 변경 이력

| 주제 | 처음 검토한 방식 | 최종 결정과 이유 |
| --- | --- | --- |
| 내부 식별자 | UUID | 하나의 관계형 DB가 식별자를 발급하므로 `BIGINT IDENTITY` 사용 |
| 카탈로그 실행 | 기동 시 한 번 | 장애 복구와 변경 반영을 위해 기동 직후와 주기 실행 |
| 사라진 매핑 | 삭제 | 재등장 시 내부 ID를 유지하도록 비활성화 |
| 빈 전체 목록 | 정상 전체 목록으로 반영 | 기존 활성 데이터가 있으면 장애 응답일 가능성을 고려하여 반영 보류 |
| 최초 동기화 스레드 | `ApplicationReadyEvent` 동기 실행 | 준비 완료 흐름을 막지 않도록 스케줄러 스레드에서 즉시 실행 |
| 객실 타입 조회 | 숙소마다 조회 | Supplier 단위 일괄 조회 후 숙소 ID로 그룹화 |
| Offer 생성과 격리 | public record와 완성된 후보만 보호 | private 생성자와 원본 항목 변환 전체의 후보별 격리 |
| 목록 항목 누락 | 정상 snapshot에서 즉시 비활성화 | 한 번의 부분 응답 가능성을 고려해 정상 snapshot 2회 연속 누락 시 비활성화 |

## 2026-09-12 - 도메인과 연동 정책 수립

### 수행 내용

- 전체 요구사항과 두 Supplier 계약을 검토했다.
- Java 21과 Spring Boot로 생성된 초기 프로젝트 상태를 확인했다.
- 숙소, 객실 타입, Offer, 가격, 재고의 내부 경계를 정의했다.
- 고객 응답 상태와 Supplier 실패 처리 방식을 정했다.
- 카탈로그 동기화, 식별자 유지, 타임아웃, 요청 분할, 동시 호출 정책을 정했다.
- 단계별 구현 순서와 커밋 계획을 작성했다.

이 단계에서는 제품 코드를 구현하지 않았다. 코드를 작성하기 전에 변경 비용이 큰 판단을 먼저 명확히 하는 것이 목적이었다.

### 결정과 근거

- 공통 식별자가 없고 추정 병합의 위험이 크므로 Supplier가 다른 숙소는 별도로 관리한다.
- 가격, 재고, 조식 조건이 붙는 단위이므로 객실 타입의 판매 조건을 Offer로 정의한다.
- 내부 처리는 Offer 단위로 유지하면서 고객 응답은 숙소와 객실 타입별로 그룹화한다.
- 현재 하나의 관계형 DB만 사용하므로 내부 ID는 DB 생성 숫자를 사용한다.
- 가격은 세금 포함 숙박 전체 총액으로 통일하고 Supplier가 제공한 날짜별 상세만 내부에서 보존한다.
- 검색 응답 크기와 Supplier 간 일관성을 위해 날짜별 가격은 상품 상세 확장으로 미룬다.
- 검색 목적을 예약 가능한 상품 조회로 정의하여 품절 Offer를 제외한다.
- 해석 가능한 응답에서 일부 Offer만 잘못됐다면 정상 형제 Offer를 보존한다.
- 사용할 수 있는 부분 결과에는 HTTP 200을, 모든 Supplier 실패에는 HTTP 503을 사용한다.
- 카탈로그는 기동 시와 주기적으로 동기화하고 실패 중에도 기존 매핑을 보존한다.
- 다시 등장한 상품의 내부 ID를 유지하기 위해 비활성 매핑을 삭제하지 않는다.
- 고객 응답 시간과 장애 중 호출량을 고려하여 첫 검색 구현에서는 재시도하지 않는다.
- Supplier 원천이 날짜와 인원에 의존하는 요청 방식이므로 검색에 WebSocket, MQ, Kafka를 사용하지 않는다.
- 고객 요청의 50개 분할은 영속 배치 작업이 아니므로 Spring Batch를 사용하지 않는다.
- 무제한 fan-out과 순차 호출 사이에서 Supplier별 제한 병렬성을 선택한다.
- 환율과 결제 정책이 없으므로 원본 통화를 보존하고 변환하지 않는다.
- JPA와의 일관성을 위해 Spring MVC를 사용하고 Supplier HTTP 호출만 `WebClient`로 병렬 처리한다.

### 검토 후 수정하거나 제외한 대안

- 내부 ID로 UUID를 먼저 검토했지만 분산 ID 생성 요구가 없어 숫자 ID로 변경했다.
- 기동 시 한 번만 목록을 동기화하는 방식을 먼저 검토했지만, 장애 복구 후에도 상품이 계속 누락되는 문제가 있어 주기 동기화를 추가했다.
- 평평한 Offer 응답을 검토했다. 내부 모델은 Offer를 유지하되 고객 응답은 숙소와 객실 타입 기준으로 묶기로 했다.
- 검색 응답에서 날짜별 가격을 제공하는 방식을 검토했지만 전체 숙소 검색의 응답 크기를 고려해 상품 상세 확장으로 미뤘다.
- 목록에서 사라진 매핑을 삭제하는 방식은 재등장 시 내부 ID가 바뀌므로 제외했다.
- 하나의 잘못된 항목 때문에 Supplier 응답 전체를 버리는 방식은 독립 검증 가능한 정상 Offer를 잃으므로 제외했다.
- 메시징과 스트리밍 기반 재시도는 메시지 전달과 Supplier 응답 성공이 같은 보장이 아니며 검색 결과의 최신성도 떨어뜨리므로 제외했다.

### AI 활용 기록

AI를 사용해 도메인 경계, API 응답 구조, 내부 식별자, 실패 응답, 동기화 복구, 재시도 방법, 구현 순서를 비교했다.

제안을 그대로 채택하지 않고 현재 범위에 맞는지 대화로 검토했다. UUID 제안은 하나의 DB라는 조건을 적용해 숫자 ID로 수정했다. 기동 시 한 번만 동기화하는 제안은 사용 중 Supplier가 계속 누락되는 문제를 발견해 주기 복구 방식으로 수정했다. WebSocket, MQ, Kafka, Spring Batch는 현재 Supplier 계약과 검색 최신성에 맞지 않아 제외했다.

확정한 근거와 제외한 대안은 `docs/architecture-decisions.md`에 정리했다. 이후 생성되는 코드는 각 결정을 실제로 반영하는지 검토하고, 이해하거나 설명할 수 없는 구현은 유지하지 않는다.

### 다음 작업

1. 기본 빌드를 확인하고 PostgreSQL과 migration을 설정한다.
2. 별도 포트의 최소 Mock Supplier를 구현한다.
3. 카탈로그 매핑과 동기화를 구현한다.
4. `docs/implementation-plan.md` 순서대로 검색 흐름을 완성한다.

## 2026-09-13 - 기반 환경 구성

### 수행 내용

- PostgreSQL 로컬 실행 환경을 Compose로 구성했다.
- Flyway가 스키마 변경을 전달하도록 설정했다.
- Hibernate는 스키마 생성 대신 검증만 수행하도록 설정했다.
- 테스트가 개발자 로컬 DB에 의존하지 않도록 Testcontainers PostgreSQL을 연결했다.
- 애플리케이션 실행 모델을 Spring MVC로 명시했다.

### 의사결정

- 제품 DB와 테스트 DB의 동작 차이를 줄이기 위해 H2 대신 PostgreSQL Testcontainers를 사용했다.
- 로컬 애플리케이션 실행은 상태를 유지할 수 있는 Compose DB를 사용하고, 테스트는 매번 격리되는 임시 DB를 사용한다.
- 스키마 변경 이력을 코드와 함께 추적하기 위해 Hibernate 자동 생성 대신 Flyway를 사용한다.

### 검증 결과

- `./gradlew test`: 성공
- `docker compose up -d --wait`: 성공
- `/actuator/health`: `UP`

## 2026-09-13 - Mock Supplier 구성

- 실제 애플리케이션과 다른 포트에서 실행되는 Mock 모듈을 추가했다.
- 두 Supplier의 목록 및 재고·요금 API를 고정 응답으로 구성했다.
- 정상, HTTP 오류, 본문 오류, 무응답 상황을 재현할 수 있게 했다.
- 후속 연동 개발에서 실제 HTTP 통신과 부분 실패를 검증하기 위해 Mock을 먼저 구현했다.

### 검증

- `./gradlew test`: 성공
- 정상, 장애, 무응답 시나리오 확인

## 2026-09-13 - 카탈로그 매핑 영속화

### 수행 내용

- Supplier 숙소와 객실 타입 코드를 내부 숫자 ID에 연결하는 테이블과 Entity를 추가했다.
- 숙소는 Supplier와 숙소 코드 조합, 객실 타입은 내부 숙소와 객실 타입 코드 조합으로 중복을 막았다.
- 동기화는 비활성 매핑도 찾을 수 있고 검색은 숙소와 객실 타입이 모두 활성인 매핑만 읽도록 Repository 조회를 분리했다.
- 외부 목록 DTO와 동기화 정책은 다음 단계에서 구현할 수 있도록 매핑의 생성, 갱신, 비활성화 동작만 열어 두었다.

### 의사결정

- 내부 식별자는 하나의 관계형 DB가 발급하므로 `BIGINT IDENTITY`를 사용한다.
- 객실 타입 코드는 숙소 안에서만 유일하므로 전체 Supplier 범위의 고유 제약을 두지 않는다.
- 비활성 매핑을 삭제하지 않아 재등장 시 기존 ID를 다시 사용할 수 있게 한다.
- 스키마 생성은 Hibernate가 아니라 Flyway migration이 담당하고 Hibernate는 코드와 스키마의 일치 여부만 검증한다.

### 검증

- 동일 Supplier 숙소 코드 중복 저장 거부
- 서로 다른 Supplier의 동일 숙소 코드 저장 허용
- 동일 숙소의 객실 타입 코드 중복 저장 거부
- 서로 다른 숙소의 동일 객실 타입 코드 저장 허용
- 비활성 매핑의 동기화 조회 허용
- 비활성 숙소 또는 객실 타입의 검색 조회 제외

## 2026-09-13 - Supplier 목록 연동과 동기화

### 수행 내용

- 두 Supplier의 목록 API를 호출하는 `WebClient` 어댑터를 추가했다.
- Supplier별 응답 DTO를 공통 카탈로그 snapshot으로 변환하고, 필수 값과 코드 중복을 반영 전에 검증했다.
- 정상적인 전체 snapshot을 Supplier 단위 트랜잭션으로 반영하도록 구현했다.
- 기존 매핑은 갱신·재활성화하고 목록에서 사라진 매핑은 삭제하지 않고 비활성화했다.
- 애플리케이션 기동 직후와 설정된 주기에 동기화를 실행하도록 구성했다.
- 일시적 네트워크·서버 오류만 제한적으로 재시도하고, 한 Supplier가 실패해도 다른 Supplier 동기화는 계속하도록 했다.

### 의사결정

- HTTP 호출은 트랜잭션 밖에서 완료하고, 검증을 통과한 snapshot의 DB 반영 구간만 트랜잭션으로 묶는다.
- 전체 목록이 아니거나 구조적으로 잘못된 응답은 반영하지 않아 기존 매핑이 잘못 비활성화되는 것을 막는다.
- 기존 활성 숙소 또는 객실 타입이 있는데 0건 목록이 오면 장애 응답일 가능성을 고려하여 전체 반영을 보류한다.
- 호출 한도 오류는 안전한 대기 시간을 알 수 없으므로 현재 단계에서는 즉시 재시도하지 않는다.
- 연결 500ms, 응답 3초, 재시도 2회, 300ms 백오프, 동기화 주기 10분을 초기 설정값으로 두고 환경 변수로 변경할 수 있게 했다.
- 최초 동기화도 스케줄러 전용 스레드에서 실행하여 애플리케이션 준비 완료 흐름을 막지 않게 했다.
- Supplier의 기존 객실 타입을 한 번에 조회하고 숙소별로 그룹화하여 숙소 수에 비례하던 조회를 제거했다.

### 검증

- Supplier별 요청 경로와 API 키 헤더 확인
- Supplier별 성공·실패 응답 변환 확인
- 일시적 오류 재시도와 영구 오류 미재시도 확인
- 동일 목록 재동기화와 이름 변경 후 내부 ID 유지 확인
- 누락 매핑 비활성화와 재등장 시 기존 ID 재활성화 확인
- 한 Supplier 실패가 다른 Supplier 반영을 막지 않는지 확인
- 장애성 빈 숙소·객실 타입 목록이 기존 활성 매핑을 제거하지 않는지 확인
- 예상하지 못한 저장 오류의 스택트레이스 기록 확인
- 최초 동기화가 별도 스케줄러 스레드에서 실행되는지 확인
- `./gradlew test`: 20개 테스트 성공

## 2026-09-13 - 검색 조건과 Offer 도메인

### 수행 내용

- 체크아웃일을 제외하는 검색 기간과 성인·아동 인원 검증을 구현했다.
- 통화의 최소 단위 정수를 `long`으로 보존하는 `Money`를 추가했다.
- 날짜별 세전 금액과 세금을 합산하는 가격과 세금 포함 전체 총액 가격을 하나의 `Price` 의미로 통일했다.
- 요청 숙박일의 누락·중복·범위 초과를 검증하고 일별 재고 최솟값으로 연박 재고를 계산했다.
- 예약 가능한 항목만 `Offer`로 만들고 품절과 잘못된 후보를 서로 다른 결과로 분류했다.
- 원본 항목의 DTO 변환부터 Offer 생성까지 후보별로 격리하여 하나가 잘못되어도 정상 형제 후보를 유지하는 정규화 경계를 추가했다.

### 의사결정

- 공통 가격은 객실 1실의 세금 포함 숙박 전체 금액이며 날짜별 상세는 Supplier가 제공한 경우에만 보존한다.
- 금액 합산 시 `int` 범위를 넘을 수 있으므로 내부 금액은 `long`을 사용하고 오버플로를 허용하지 않는다.
- 통화가 다른 금액은 합산하지 않고 원본 통화를 유지한다.
- 품절은 정상적인 검색 결과이므로 정규화 실패와 별도로 집계한다.
- 성인 최소 1명, 과거 날짜 제한, 최대 숙박 기간은 주어진 제품 정책에 없으므로 추가하지 않았다.

### 검토 후 보완

- 처음에는 이미 생성된 `Price`를 가진 후보만 정규화 구간에서 보호했다. 잘못된 통화나 음수 금액은 후보 생성 전에 실패한다는 문제를 확인하여, 원본 항목을 후보로 바꾸는 함수까지 보호 구간 안에서 실행하도록 변경했다.
- 처음에는 `Offer`를 public 생성자가 있는 record로 만들었다. 검증 팩토리를 우회할 수 있어 private 생성자를 가진 불변 클래스로 변경했다.
- 검증 전 원본처럼 null을 허용하던 `OfferCandidate`를 완전한 도메인 후보로 정의하고 생성 시 필수 값을 검증하도록 변경했다.
- 정규화 예외를 개수로만 집계하던 구현을 수정하여 application 계층에서 Supplier, 항목 순번, 내부 식별자와 스택트레이스를 남기도록 했다.
- Supplier의 `PARTIAL` 상태와 공통 실패 유형은 Offer 도메인이 아니라 후속 통합 검색 서비스가 계산하는 책임임을 문서에 명확히 했다.

### 검증

- 체크아웃일 제외 날짜 범위와 인원 경계값 확인
- 날짜별 세전가와 세금의 숙박 전체 총액 합산 확인
- 세금 포함 전체 총액을 재계산하지 않는지 확인
- 날짜 누락·중복·범위 초과와 음수 재고·금액 거부 확인
- `3, 1, 5`의 연박 재고가 1인지 확인
- 하루라도 재고가 0이면 Offer가 제외되는지 확인
- 잘못된 Offer와 정상 형제 Offer가 격리되는지 확인
- 호출 출처와 후보 Supplier가 다르면 거부하고 두 값을 모두 기록하는지 확인
- `./gradlew test`: 42개 테스트 성공

## 2026-09-13 - Supplier 실시간 검색 어댑터

### 수행 내용

- Supplier A의 `/a/v1/availability`와 Supplier B의 `/b/api/search` 호출 어댑터를 구현했다.
- 검색 조건과 Supplier별 활성 숙소·객실 타입 매핑을 공통 호출 계약으로 정의했다.
- Supplier A의 날짜별 세전가와 세금을 숙박 전체 총액으로 합산하고, Supplier B의 세금 포함 총액은 재계산하지 않고 보존했다.
- Supplier B의 HTTP 200 본문 실패 코드와 Supplier A의 HTTP 오류를 공통 실패 유형으로 변환했다.
- 응답 항목을 DTO에서 `Money`, `Price`, `OfferCandidate`, `Offer`로 바꾸는 전 구간을 후보별로 격리했다.
- 카탈로그와 검색의 WebClient 제한 시간을 분리하고 검색에 연결 500ms, 응답 2초, 호출 전체 3초를 적용했다.

### 의사결정

- 검색 어댑터는 활성 매핑을 직접 조회하지 않고 통합 검색 서비스가 전달한 1~50개 매핑 배치만 사용한다. 다음 단계에서 조회, Supplier별 그룹화, 50개 분할 책임을 한곳에 모으기 위해서다.
- 응답 전체를 해석할 수 없는 오류는 호출 실패로, 특정 항목에 한정된 필드·통화·금액·재고·매핑 오류는 해당 Offer 거부로 처리한다.
- Supplier 전송 DTO는 어댑터 내부 비공개 record로 두어 외부 계약 변경이 검색 도메인으로 퍼지지 않게 했다.
- 실시간 검색은 고객 응답 시간과 장애 중 호출량을 우선하여 재시도하지 않는다.
- 카탈로그와 검색은 같은 Supplier URL과 API 키를 사용하지만 서로 다른 응답 제한 시간이 필요하므로 WebClient 설정을 분리했다.

### 검증

- Supplier별 요청 경로, query parameter, API 키 헤더 확인
- Supplier A 날짜별 가격 합산과 일별 재고 최솟값 확인
- Supplier B 세금 포함 총액 보존과 일별 재고 최솟값 확인
- 알 수 없는 매핑이 정상 형제 Offer를 제거하지 않는지 확인
- 세금 미포함 Supplier B 항목이 Offer 단위로 거부되는지 확인
- Supplier B 본문 오류와 Supplier A HTTP 오류의 공통 실패 변환 확인
- 무응답이 호출 전체 제한 시간 안에 `TIMEOUT`으로 종료되고 재시도되지 않는지 확인
- 50개 초과 호출 묶음과 중복 객실 타입 코드 거부 확인
- `./gradlew test`: 51개 테스트 성공

## 2026-09-13 - 연동 장애 재현과 설정 계약 보완

### 수행 내용

- 실행 중인 Mock Supplier의 A/B 검색 모드를 `normal`, `error`, `no-response`로 변경하는 상태 제어 API를 추가했다.
- 요청 헤더 방식은 개별 테스트 override로 유지하고, 저장된 장애 모드는 실시간 검색 API에만 적용하여 카탈로그 매핑을 안정적으로 확보할 수 있게 했다.
- Mock 검색 응답의 가격과 재고 날짜를 요청한 체크인부터 체크아웃 전날까지 동적으로 생성하도록 변경했다.
- Mock은 요청받은 숙소 코드에 해당하는 항목만 반환하도록 보완했다.
- `CATALOG_UNAVAILABLE`을 공통 실패 enum에 추가하여 설계 문서와 코드의 실패 분류를 일치시켰다.
- Supplier 설정에 Bean Validation을 적용하고 카탈로그 `initialDelay`를 설정 계약에 포함했다.
- 메인 애플리케이션의 Servlet 실행 모델을 설정에 명시하고 기본값과 같은 정적 리소스 설정을 제거했다.

### 의사결정

- 무응답 지연은 검색 제한 시간을 충분히 넘는 30초로 줄이고 Mock에서 virtual thread를 사용한다. 반복 시연으로 플랫폼 스레드가 장시간 점유되는 위험을 줄이기 위해서다.
- 카탈로그 동기화는 백그라운드에서 실행되고 현재 Supplier가 두 개이므로 이번 보완에서는 순차 실행을 유지한다. 대규모 Supplier 추가나 동기화 지연이 관측되면 Supplier 단위 병렬화를 검토한다.
- Supplier당 3,000개를 동시성 4로 조회하면 60묶음, 15 wave가 필요하므로 묶음 p95 1초에서는 5초 예산 안에 전체 완료할 수 없음을 설계 문서에 명시했다.
- 현재 계약은 전체 숙소 조회이므로 임의로 대상을 줄이지 않고, 호출 한도 내 동시성 조정과 bulk 계약 변경을 우선 확장안으로 둔다.
- 다중 인스턴스 잠금, 카탈로그 병렬화, 추가 인덱스, 대량 쓰기, 동기화 상태 테이블, Outbox는 도입 조건과 검증 방법을 설계 문서의 후속 확장 목록에 남기고 근거 없이 선구현하지 않는다.
- 실행 명령을 포함한 `README.md`는 고객 API와 장애 시나리오가 완성되는 9단계에서 실제 동작을 기준으로 작성한다.

### 검증

- 상태 제어 API의 모드 저장과 Supplier별 독립 적용 확인
- 검색 장애 모드에서도 카탈로그 API가 정상 응답하는지 확인
- 요청 헤더가 저장된 모드를 요청 단위로 덮어쓰는지 확인
- Supplier B 장애 모드가 HTTP 200과 `E503` 본문으로 응답하는지 확인
- 임의의 검색 날짜와 요청 숙소 코드에 맞는 동적 응답 생성 확인
- 잘못된 Supplier 또는 모드 변경 요청의 HTTP 400 확인
- 유효한 설정과 잘못된 API 키·타임아웃의 Bean Validation 확인
- `./gradlew test`: 메인·Mock 모듈 합계 59개 테스트 성공

## 2026-09-14 - 6단계 전 연동 안정성 보완

### 수행 내용

- Supplier A와 B의 HTTP 오류 상태를 하나의 공통 mapper로 변환하도록 통일했다.
- Supplier B의 HTTP 400, 401, 429가 각각 `INVALID_REQUEST`, `AUTHENTICATION_FAILED`, `RATE_LIMITED`로 분류되도록 수정했다.
- 카탈로그 호출 한 번의 전체 제한 시간을 추가하고, 타임아웃도 기존 일시 장애 재시도 정책을 따르게 했다.
- 카탈로그 스케줄러의 활성화 조건과 애플리케이션 전역 스케줄링 활성화를 분리했다.

### 의사결정

- Supplier별 전송 계약은 각 어댑터에 유지하되 HTTP 상태의 공통 의미만 재사용한다.
- 카탈로그 `responseTimeout`과 별도로 reactive 호출 전체를 제한하는 `callTimeout`을 둔다. 초기값은 응답 제한보다 긴 4초로 설정한다.
- `supplier.catalog.enabled=false`는 카탈로그 작업만 비활성화하며 애플리케이션의 다른 스케줄 작업에는 영향을 주지 않는다.
- 부분 카탈로그 급감 방어는 허용 감소 비율이나 확인 절차라는 제품 정책이 필요하므로 이번 변경에 임의의 임계값을 추가하지 않았다.

### 검증

- Supplier B 카탈로그 HTTP 401과 검색 HTTP 429의 공통 실패 분류 확인
- 카탈로그 호출별 타임아웃과 일시 오류 재시도 2회 확인
- 카탈로그 스케줄러 비활성화 상태에서도 전역 스케줄링 인프라가 유지되는지 확인
- `./gradlew test`: 메인 57개, Mock 6개, 합계 63개 테스트 성공

## 2026-09-14 - 카탈로그 연속 누락 확인 정책

### 수행 내용

- 숙소와 객실 타입 매핑에 `consecutive_missing_count`를 추가하고 Flyway migration으로 기존 데이터는 0부터 시작하게 했다.
- 정상 snapshot에서 처음 누락된 매핑은 활성 상태로 유지하고, 두 번 연속 누락된 경우에만 비활성화하도록 변경했다.
- 누락된 매핑이 다시 등장하면 내부 ID를 유지한 채 활성화하고 누락 횟수를 0으로 초기화했다.
- 숙소 전체가 누락된 경우에는 숙소에만 누락을 기록하고, 두 번째 누락으로 숙소가 비활성화될 때 하위 객실 타입도 검색에서 제외되도록 했다.
- 동기화 성공 로그에 숙소·객실 수와 의심 누락, 비활성화, 재활성화 건수를 남기도록 했다.

### 의사결정

- Supplier는 상품 정보의 원천으로 신뢰하지만, HTTP 성공 한 번만으로 전체 목록의 완전성까지 확정하지 않는다.
- 호출 실패, 타임아웃, 구조적으로 거부된 snapshot은 저장 경계를 통과하지 않으므로 누락 횟수를 변경하지 않는다.
- 첫 누락 매핑은 실시간 검색 호출 대상에는 남지만, Supplier가 판매 가능한 Offer를 주지 않으면 고객 결과에는 노출되지 않는다. 반대로 유효한 매핑을 너무 일찍 끄면 정상 Offer를 조회할 기회 자체를 잃으므로 2회 확인을 선택했다.
- 대량 비활성화 비율 제한과 관리자 승인 흐름은 정상 변동 폭을 관측할 데이터와 운영 화면이 필요한 후속 확장으로 남겼다.

### 검증

- 숙소와 객실 타입 각각 첫 누락 시 활성 유지 및 누락 횟수 1 기록 확인
- 두 번째 연속 누락 시 비활성화 및 검색 조회 제외 확인
- 중간에 다시 등장하면 누락 횟수가 초기화되어 다음 한 번의 누락으로 비활성화되지 않는지 확인
- 재활성화 후 숙소와 객실 타입의 기존 내부 ID 유지 확인
- Supplier별 누락 상태가 독립적으로 반영되는지 확인
- `./gradlew test`: 메인 59개, Mock 6개, 합계 65개 테스트 성공

## 2026-09-14 - 제한 병렬 통합 검색

### 수행 내용

- 활성 숙소·객실 타입의 내부 ID와 Supplier 코드만 읽는 카탈로그 projection을 추가했다.
- 활성 매핑을 Supplier별로 그룹화하고 숙소 50개 단위의 검색 요청으로 분할했다.
- 서로 다른 Supplier는 동시에 구독하고, 한 Supplier 안에서는 설정된 수만큼만 배치를 병렬 호출하도록 구현했다.
- 배치 오류를 공통 실패 유형으로 모으면서 성공한 배치의 Offer, 거부 건수, 품절 건수를 보존했다.
- 전체 검색 제한 시간이 끝나면 실행·대기 중인 호출을 취소하고 미완료 배치를 `TIMEOUT`으로 집계했다.
- Supplier별 `SUCCESS`, `PARTIAL`, `FAILED`와 전체 `COMPLETE`, `PARTIAL`, `FAILED` 상태를 계산했다.
- 활성 카탈로그가 없는 Supplier는 호출하지 않고 `CATALOG_UNAVAILABLE`로 표시했다.
- 직접 만든 `WebClient.Builder` 대신 Spring Boot가 자동 구성한 빌더를 주입하도록 변경했다.

### 의사결정

- 전체 제한 시간이 발생해도 `timeout` 예외로 완료 결과 전체를 버리지 않고, 시간 안에 도착한 배치를 수집한 뒤 나머지만 실패로 계산한다.
- Supplier가 정상적으로 빈 결과를 반환한 경우는 성공이다. 다른 Supplier가 실패했다면 전체 결과는 Offer가 없어도 `PARTIAL`이다.
- 잘못된 Offer가 하나 이상 격리되면 해당 Supplier는 `PARTIAL`과 `INVALID_RESPONSE`로 표시하지만, 정상 품절 건수만으로는 실패 상태를 만들지 않는다.
- 동시성 설정은 현재 고객 요청별·Supplier별 제한이다. 여러 고객 요청을 합친 전역 bulkhead는 운영 부하 지표가 필요한 8단계로 남긴다.
- Spring Boot 4에서 WebClient 자동 구성과 Observation customizer를 적용하기 위해 서버용 WebFlux starter 대신 전용 WebClient starter를 사용한다.

### 검증

- 숙소 51개가 50개와 1개 요청으로 나뉘는지 확인
- Supplier A와 B가 어느 한쪽 완료를 기다리지 않고 시작하는지 확인
- 한 Supplier의 실제 동시 호출 수가 설정값 4를 넘지 않는지 확인
- 한 배치 실패와 전체 제한 시간에도 완료 Offer가 보존되는지 확인
- 정상 빈 결과와 전 Supplier 실패의 전체 상태 계산 확인
- 활성 카탈로그가 없을 때 외부 호출 없이 실패 원인이 기록되는지 확인
- 카탈로그 projection이 비활성 매핑을 제외하는지 확인
- 검색 전체 제한 시간과 동시성 설정의 Bean Validation 확인
- `./gradlew test`: 메인 71개, Mock 6개, 합계 77개 테스트 성공

## 2026-09-14 - 통합 검색 리뷰 반영과 카탈로그 급감 방어

### 수행 내용

- 요청 인원을 수용하지 못하는 객실을 잘못된 Supplier 데이터가 아닌 정상 검색 제외로 변경했다.
- Supplier별 `enabled` 설정을 추가하고, 비활성 Supplier를 카탈로그 동기화·실시간 검색·결과 집계에서 제외했다.
- 활성 Supplier에 검색 client가 없으면 애플리케이션 생성 시점에 실패하도록 검증했다.
- 활성 카탈로그 조회를 application 포트와 JPA 어댑터로 분리하고, projection 조회만 짧은 read-only 트랜잭션에서 수행하도록 했다.
- 검색 배치를 미리 전부 만들지 않고 제한 병렬 구독에 맞춰 생성하며, DB 반환 순서와 무관하게 매핑을 명시적으로 정렬하도록 했다.
- 활성 숙소 또는 객실 타입이 10개 이상 누락되고 누락률이 50%를 초과하면 snapshot 전체를 거부하는 안전장치를 추가했다.
- 사용하지 않는 Entity 검색 조회를 제거하고 테스트 starter와 `WebClient.Builder` 사용을 정리했다.

### 의사결정

- 계약 종료나 계획된 점검은 장애가 아니므로 영구 `PARTIAL`로 표현하지 않고 명시적인 운영 설정으로 조회 대상에서 제외한다.
- 활성 Supplier와 client 구성 불일치는 요청 중 예외가 아니라 배포 구성 오류이므로 시작 시점에 발견한다.
- 카탈로그 read-only 트랜잭션을 통합 검색 전체에 적용하면 외부 HTTP 호출 동안 DB 연결을 붙잡을 수 있으므로 JPA 조회 어댑터 경계에만 둔다.
- 두 번 연속 누락은 작은 변동의 오탐을 줄이고, 절대 수와 비율을 함께 보는 급감 가드는 3,000개가 1개로 줄어드는 대형 부분 응답을 첫 회에 격리한다.
- 10개·50%는 현재 데이터가 없는 상태의 보수적인 초기값이며 환경 설정으로 노출한다. 운영 관측 후 Supplier별 임계값과 관리자 승인 흐름을 검토한다.

### 검증

- 수용 인원 부족이 `rejectedOfferCount`나 `PARTIAL`을 만들지 않고 정상 제외로 집계되는지 확인
- 비활성 Supplier의 매핑과 client가 호출 및 결과에서 제외되는지 확인
- 활성 Supplier에 client가 없으면 서비스 생성이 거부되는지 확인
- 숙소 20개→1개, 객실 타입 20개→1개 snapshot이 기존 매핑을 보존한 채 거부되는지 확인
- 기존 분할·동시성·부분 성공·전체 제한 시간 회귀 확인
- `./gradlew test`: 메인 76개, Mock 6개, 합계 82개 테스트 성공

## 2026-09-15 - 통합 검색 리뷰 후 경계 조건 보완

### 수행 내용

- 기존 활성 숙소 또는 객실 타입이 전부 누락되면 카탈로그 규모와 무관하게 snapshot 전체를 거부하도록 급감 가드를 보완했다.
- 쉼표로 숙소 코드를 전달하는 현재 Supplier 계약에서 구분자와 충돌하는 숙소 코드를 카탈로그 수신 경계에서 거부하고 검색 요청 경계에서도 다시 확인했다.
- 프로덕션과 테스트 어디에서도 사용하지 않는 활성 숙소 Repository 조회를 제거했다.
- `supplier.<id>.enabled=false`가 외부 호출과 결과 집계만 중단하고 기존 DB 매핑은 보존한다는 의미를 설계 문서에 명확히 했다.

### 의사결정

- 절대 누락 수 임계값은 작은 카탈로그의 정상 변동을 허용하기 위한 값이지만, 기존 활성 집합 전체가 다른 코드로 대체되는 상황은 규모와 무관하게 장애성 응답으로 본다.
- Supplier 전체 활성 객실 매핑의 급감은 고객에게 노출 가능한 판매 단위 손실이므로, 통째로 사라진 숙소의 객실도 객실 타입 비율 계산에 계속 포함한다.
- Supplier 비활성 설정은 점검용 운영 스위치로 정의한다. 계약 종료에 따른 매핑 비활성화와 재활성화 전 최신 동기화 보장은 별도 운영 절차로 분리한다.

### 검증

- 기존 숙소 3개가 전혀 다른 숙소 1개로 대체되는 snapshot 거부 확인
- 같은 숙소의 기존 객실 타입 3개가 전혀 다른 객실 타입 1개로 대체되는 snapshot 거부 확인
- 쉼표가 포함된 Supplier 숙소 코드의 카탈로그 snapshot 생성 거부 확인
- 쉼표가 포함된 Supplier 숙소 코드의 검색 요청 생성 거부 확인
- `./gradlew test`: 메인 80개, Mock 6개, 합계 86개 테스트 성공

## 2026-09-15 - 고객 통합 검색 API

### 수행 내용

- `GET /api/v1/stays/search`를 추가하고 체크인·체크아웃 날짜와 성인·아동 수를 검색 조건으로 받도록 했다.
- 통합 검색 Offer를 숙소, 객실 타입, Supplier Offer 순서의 고객 응답으로 조립했다.
- 검색용 카탈로그 projection에 숙소명과 객실 타입명을 포함하여 외부 호출이 끝난 뒤 Entity 재조회 없이 응답을 완성했다.
- 전체 검색 상태와 Supplier별 성공·부분 성공·실패, 수용·거부·정상 제외 건수, 공통 실패 유형을 응답에 포함했다.
- 잘못된 날짜·인원 조건과 누락되거나 형식이 틀린 파라미터를 안정적인 HTTP 400 오류 본문으로 변환했다.
- 모든 조회 대상 Supplier가 실패하면 상세 실패 본문을 유지한 채 HTTP 503을 반환하도록 했다.

### 의사결정

- 고객 응답에는 내부 숙소·객실 타입 ID와 이름만 노출하고 Supplier 원본 숙소·객실 코드는 포함하지 않는다.
- 부분 실패는 조회 가능한 상품을 버리지 않고 HTTP 200과 `PARTIAL`로 표현하며, 전체 실패만 HTTP 503과 `FAILED`로 구분한다.
- 정상적으로 판매 가능한 Offer가 없는 경우에는 `null` 대신 빈 배열을 반환하고 성공 상태를 유지한다.
- API 응답 조립에 필요한 이름은 외부 호출 전에 같은 read-only projection으로 읽어 DB 트랜잭션을 다시 열거나 외부 호출 동안 유지하지 않는다.

### 검증

- 정상 결과가 숙소와 객실 타입 기준으로 묶이고 요구 필드가 모두 반환되는지 확인
- Supplier 원본 숙소·객실 코드가 JSON 어디에도 노출되지 않는지 확인
- 부분 실패는 HTTP 200, 전체 실패는 실패 상세를 포함한 HTTP 503으로 반환되는지 확인
- 정상 빈 결과에서 `stays`와 실패 유형이 빈 배열로 직렬화되는지 확인
- 날짜 역전과 누락·형식 오류가 Supplier 호출 전에 HTTP 400으로 종료되는지 확인
- `./gradlew test`: 메인 86개, Mock 6개, 합계 92개 테스트 성공

## 2026-09-15 - 고객 검색 결과 경량화와 요청 상한

### 수행 내용

- 전체 활성 카탈로그를 `IntegratedSearchResult`에 복사하던 구조를 제거했다.
- 실제로 수용된 Offer만 숙소명·객실 타입명과 결합하는 application 타입 `SearchOffer`를 추가했다.
- API 계층에서 평행한 Offer 목록과 카탈로그 목록을 수동 조인하던 로직을 제거했다.
- 최대 숙박 기간을 30박으로 제한하고 `children`을 생략하면 0명으로 처리하도록 했다.
- API 가격 필드를 `totalAmountIncludingTax`로 변경하여 세금 포함 숙박 전체 총액이라는 의미를 명시했다.
- 응답 record의 필수 값과 컬렉션에 명시적인 null 검증 메시지를 추가했다.

### 의사결정

- 활성 매핑 전체는 Supplier 요청 조립에 필요하지만 응답 조립에는 반환된 Offer의 메타데이터만 사용한다.
- 반환 Offer ID 집합으로 카탈로그 행을 한 번 필터링하고 별도 정렬 없이 결합하여, 응답용 객체와 검증 비용이 전체 카탈로그 크기가 아닌 실제 Offer 수에 비례하도록 했다.
- 카탈로그 조인 불일치를 API가 표현 가능한 정상 상태로 두지 않고 application 서비스의 내부 불변식 위반으로 제한했다. 이 불변식이 깨지면 숨기지 않고 서버 오류로 처리하며 8단계 관측 대상에 포함한다.
- 30박 상한은 제공되지 않은 상품 정책을 추정하려는 목적이 아니라 공개 API 한 요청의 자원 사용량을 제한하는 초기 안전값이며, 실제 정책이 주어지면 조정한다.

### 검증

- 활성 매핑 51개에서 Offer가 없을 때 응답용 `SearchOffer`가 생성되지 않는지 확인
- 일부 배치에서 반환된 Offer만 올바른 숙소명·객실 타입명과 결합되는지 확인
- 30박은 허용하고 31박은 Supplier 호출 전에 HTTP 400으로 거부되는지 확인
- `children` 생략 시 0명으로 검색되는지 확인
- 세금 포함 총액 필드명과 Supplier 코드 비노출 계약 확인
- `./gradlew test`: 메인 89개, Mock 6개, 합계 95개 테스트 성공

## 2026-09-15 - 중간 기술 감사: 요구사항과 정확성

### 수행 내용

- 7단계 리뷰 반영 커밋을 기준으로 통합 모델, 매핑 생명주기, Supplier 요청·응답, 실패 상태와 테스트를 대조했다.
- 결과를 `docs/correctness-audit.md`에 정리하고, 확인한 문제와 의도된 정책 및 미검증 항목을 구분했다.
- 제품 코드와 저장소의 테스트 소스는 변경하지 않았다. 별도 임시 소스와 Gradle init script로 의심 사례를 재현했다.

### 발견과 판단

- 실제 Spring Boot에 주입된 Supplier B client에서도 음수 소수 금액이 0으로 변환되어 유효 Offer로 수용됐다. 정수 계약을 입력 경계에서 엄격히 검사하는 작업을 최우선 수정 후보로 분류했다.
- 정상 JSON 안의 개별 날짜·타입 오류가 전체 DTO 역직렬화를 실패시켜 정상 형제 Offer를 제거했다. 숫자 검사 강화와 항목별 파싱 격리를 같은 변경에서 다뤄 재작업을 줄이기로 제안했다.
- A/B 동일 Offer 중복 수용과 A 숙소 코드의 특수문자 전송 문제를 재현했다.
- 빈 카탈로그·대량 삭제 방어, 30박 상한, 성인 없는 검색 허용은 현재 설계 근거가 있으므로 일괄 결함으로 분류하지 않았다.
- 모든 항목 거부 및 모든 Supplier 비활성 상태는 정책 명확화 대상으로 남겼다. 독립 트랜잭션 rollback과 동시 동기화는 다음 DB 감사에서 검증한다.

### 검증

- Java 21에서 `./gradlew test --rerun-tasks --console=plain`: 메인 89개, Mock 6개 성공. 실패·오류·건너뜀 0개. PostgreSQL 통합 테스트 포함.
- 임시 재현 테스트 10개 성공: A/B 중복, 항목별 파싱 실패, 소수 값 변환, 특수문자 전송, 실제 Boot codec 동작 확인.
- 임시 테스트는 현재 문제 동작을 재현하는 검증이며 정식 회귀 테스트 추가 또는 정확성 문제 해결을 의미하지 않는다.
- 전체 실제 연동 E2E, DB 실행 계획, 부하·메모리·socket 자원 반환은 이번 단계에서 검증하지 않았다.

### AI 활용

- 요청: 7단계 이후 첫 작업으로 요구사항·정확성 감사를 진행하고 다음 구현의 우선순위를 정한다.
- 활용: AI가 구현·테스트·설계 기록을 대조하고 의심 사례를 임시 테스트로 재현했다. 특히 금액 변환 문제는 수동 생성 client와 실제 Boot 설정을 구분해 추가 확인했다.
- 산출물은 수정 제안이다. 제안된 구현을 반영하거나 사용자가 설계 판단을 최종 승인한 것으로 기록하지 않는다.

## 2026-09-15 - 중간 기술 감사: DB 접근과 동시성

### 수행 내용

- `docs/database-audit.md`에 실제 SQL 수, 트랜잭션·제약, 동시 실행, 인덱스 실행 계획을 정리했다.
- 합성 데이터의 실제 SQL·EXPLAIN 계획·측정 조건과 테스트 이름을 `docs/audit-evidence/db-query-plans.json`에 보존했다.
- 별도 PostgreSQL 17.11 Testcontainers DB에서 임시 검증 12개를 실행했다. 제품 코드·migration·애플리케이션 DB는 변경하지 않았다.

### 확인한 결과

- 별도 commit을 거친 누락·재등장에서도 숙소·객실 ID가 유지됐다. DB 저장 중간 실패는 앞선 INSERT까지 rollback했고 다음 Supplier는 독립적으로 commit했다.
- 검색은 숙소 1/100/1,000개에서 SELECT 1개·Entity 로딩 0개였다. 변경 없는 동기화는 SELECT 2개·UPDATE 0개였다.
- 동일 Supplier가 겹쳐 실행되면 최초 INSERT 경쟁, 누락 횟수 갱신 유실, 과거 snapshot의 최신 상태 덮어쓰기가 재현됐다. 단일 기본 스케줄의 동작과 다중 실행 위험을 구분했다.
- 테스트 검색 예산 100ms에서 DB lock을 기다리면 384ms까지 반환하지 않았다. DB timeout과 연결 대기 예산을 8단계 검토 항목으로 확정했다.
- 숙소 6,000개·객실 30,000개에서 활성 비율별 실행 계획을 비교했다. 기존 인덱스가 선택적으로 사용됐으며 추가 부분 인덱스의 일관된 성능 개선은 확인하지 못했다.
- `property_id` 선두 인덱스는 기존 객실 unique 제약으로 이미 존재함을 확인했다.
- 임시 batching 설정에서 400행 UPDATE가 50행씩 8회 batch로 실행됐고 statement 준비 수는 402에서 4로 줄었다. 갱신 행 수와 준비 수를 구분했으며, IDENTITY INSERT는 같은 설정에서도 개별 실행됐다.

### 검증과 시행착오

- DB 기본 감사 10개와 추가 timeout·batching 비교 2개를 실행해 모두 통과했다. 실패·오류·건너뜀 0개.
- 최초 임시 테스트는 JPA repository proxy를 Mockito의 `callRealMethod`로 호출하는 방식이 맞지 않아 테스트 장치에서 실패했다. 실제 repository 반환값을 유지하는 AOP 후처리 대기 장치로 바꿔 동시 실행 순서를 제어했다. 이를 제품 결함으로 기록하지 않았다.
- 기존 95개 테스트는 직전 정확성 감사의 결과를 기준점으로 사용했다. 제품 코드 변경이 없어 이번에는 전체 테스트를 반복하지 않았다.
- EXPLAIN 수치는 warm cache의 로컬 SQL 실행 시간이다. 같은 계획에서도 시간 변동이 커 인덱스 추가의 악화·개선 비율을 단정하지 않았다.

### AI 활용과 남은 작업

- AI가 코드의 의심 지점을 실제 DB에서 재현하고 PostgreSQL·Hibernate 공식 문서로 lock, 제약·인덱스, timeout, batching의 적용 경계를 확인했다.
- 구현 변경 없이 관측한 사실과 개선 제안을 분리했다. advisory lock만으로 snapshot의 최신 순서까지 보장된다고 제안하지 않았다.
- 다음은 네트워크·메모리·전역 동시성 감사다. 앞선 금액·파싱·중복 Offer·코드 전송 문제와 이번 DB 문제의 구현은 아직 진행하지 않았다.

## 2026-09-15 - 중간 기술 감사: 네트워크·메모리·전역 동시성

### 수행 내용

- `docs/network-memory-audit.md`에 외부 호출, timeout 취소, 응답 크기, 메모리 할당과 관측성 결과를 정리했다.
- `docs/audit-evidence/network-memory.json`에 측정 수치·조건·테스트 이름·임시 검증 소스와 재실행 방법을 보존했다.
- 실제 로컬 HTTP 검증 7개, Boot/Tomcat 검증 2개, 메모리 검증 2개를 임시 소스로 실행했다. 제품 코드·설정·정식 테스트는 변경하지 않았다.

### 확인한 결과

- 숙소 251개에서 Supplier A/B 각각 호출 6회·동시 최대 4회를 확인했다. 같은 서비스를 고객 8명이 동시에 호출하면 Supplier별 최대 32회까지 늘어 요청 단위 제한임을 확인했다.
- A/B의 response timeout과 call timeout에서 상대 서버의 연결 종료를 확인했다. 전체 timeout은 완료 Offer 1개를 보존하고 진행 중 연결을 닫았으며 다음 배치를 전송하지 않았다.
- 정상 형식의 A 카탈로그 3,000개(369,797 bytes)와 A 검색 50개 숙소·5개 객실·30박(613,716 bytes)이 262,144 bytes 버퍼 한도를 초과했다. 검색 실패는 UNKNOWN으로 분류됐다.
- 실제 고객 TCP 연결을 끊은 뒤 300ms에도 외부 호출 2개가 진행 중이었다. 영구 누수로 해석하지 않고 자체 timeout·전역 허용량으로 비용을 제한할 대상으로 분류했다.
- B HTTP 200 본문 E503은 업무 결과가 UNAVAILABLE이지만 완료 HTTP metric은 SUCCESS였다. Supplier 업무 지표와 구간별 시간을 8단계 우선 작업으로 제안했다.
- 숙소 6,000개·객실 30,000개에서 빈 검색의 호출 스레드 할당량 중앙값은 약 9.80 MB였다. A Offer 1,000개·30박의 결과 객체 그래프는 약 3.24 MB였다. peak heap이나 운영 부하 수치로 해석하지 않았다.

### 판단과 검증 범위

- 임시 검증 11개 성공. 세부 예외·metric·수치 중 일부는 로그 관측으로 확인했고, 재현 성공을 제품 문제 해결로 기록하지 않았다.
- 기본 연결 풀은 실제 런타임에서 최대 500개였다. 전역 업무 제한 부재와 무제한 연결 풀을 혼동하지 않았고, 풀 자체의 45초 대기가 고객 응답 시간이라는 결론도 내리지 않았다.
- 풀 포화, DNS/TLS, CPU·GC 부하, 실제 다중 인스턴스는 미검증이다. 기존 95개 테스트는 정확성 감사의 통과 결과를 기준점으로 사용했다.
- 전역 동시 호출 제한, 유한한 응답 크기 정책·실패 분류, DB/pool을 포함한 시간 예산, 최소 관측성을 우선한다. 중복 목록·정렬·합산은 저비용 개선 후보로 분류했다.
- 전체 WebFlux 전환, 분산 제한, 캐시, circuit breaker, 정규화 scheduler 분리를 일괄 도입하지 않는다. 다음 7.5-D 구조·테스트 감사 후 8단계 구현 목록을 통합한다.

### AI 활용

- AI가 의심 경로에 합성 HTTP 서버와 실제 Boot 호출을 구성하고, instrumentation 및 스레드 할당량 측정으로 메모리 비용을 확인했다.
- Spring·Reactor Netty 공식 문서와 현재 의존성의 소스를 대조했다. 관측한 사실, 코드로 추론한 위험, 후속 개선을 분리했다.
- 이번 산출물은 감사와 제안이며 제품 수정·커밋·push는 수행하지 않았다.

## 2026-09-16 - 중간 기술 감사: 구조·SOLID·공급사 확장성·예외·테스트

이 절은 기존 문서의 초기 감사 기록이다. Supplier C 실험의 수치는 기존 기록으로 보존한다. 아래 재검토에서는 그 실험을 반복하지 않았으며, 확장 성공의 범위와 구조·예외에 대한 일부 초기 판단을 정정했다.

### 수행 내용

- 계층별 `import` 매트릭스를 만들어 경계 누수를 기계적으로 판정했다.
- `git worktree`로 기준 커밋을 격리한 뒤 Supplier C를 실제로 추가해, 컴파일러가 필수 변경 지점을 열거하게 했다.
- catch 블록 6곳을 열거했다. Reactor 오류 연산자는 초기 기록에서 13곳으로 셌으나 재검토에서 `onErrorMap` 13곳과 `onErrorResume` 1곳, 총 14곳으로 정정했다.
- `requireText` 복제, `Offer.createAvailable` 호출처, 테스트 책임 분류와 테스트 인프라를 조사했다.

### 의사결정

- 확장성은 추정하지 않고 실제로 추가해 측정한다. enum 상수만 넣었을 때 컴파일 에러는 1곳이었고, 동작까지 완성했을 때 기존 메인 파일 4개(+36/−1)만 바뀌었다. 검색 서비스·정규화·API·도메인은 수정이 필요 없었다.
- 테스트는 공급사 수에 결합되어 5개 파일 9곳이 깨졌다. 9곳 모두 Supplier C를 검증하지 않는 테스트이므로 픽스처 빌더로 해소한다.
- exhaustive switch와 `requireClientsForEnabledSuppliers` 가드는 확장 마찰의 원인이지만 컴파일·시작 시점 안전망이므로 유지한다. properties의 `Map<Supplier, Endpoint>` 전환은 권하지 않는다.
- 초기에는 네트워크 감사 N-02의 버퍼 초과로 내부 프로그래밍 오류까지 관측했다고 판단했으나 서로 다른 사례다. 아래 재검토에서 내부 예외를 별도로 주입해 확인했다.
- 카탈로그 writer의 infrastructure 이동을 초기 개선안으로 제안했다. 재검토에서는 파일 이동만으로 정책·저장 구현이 분리되지 않는다고 정정하고, DB 변경 시 검토할 선택 사항으로 낮췄다.
- 검증 호출부의 경계별 중복은 유지하고, 복제된 헬퍼 정의만 정리 대상으로 삼는다.

### 검증

- `search/domain`이 Spring·JPA·Jackson·Reactor를 전혀 의존하지 않음을 확인
- `catalog/application`만 `catalog/infrastructure`를 import함을 확인 (`CatalogSnapshotWriter`)
- Supplier C 추가 후 메인 테스트 89개 전부 통과, 실패·오류 0
- `SupplierHttpFailureMapper`·`SupplierTransportFailureMapper`를 참조하는 테스트가 0개임을 확인
- PostgreSQL 컨테이너가 3개 클래스에서 각각 기동되며 재사용 설정이 없음을 확인
- 측정 근거를 `docs/audit-evidence/structure-extensibility.json`에 보존
- 제품 코드·테스트·설정은 변경하지 않았고 실험 worktree는 제거함

## 2026-09-16 - 구조 감사 문서 재검토와 경계 재현

### 문서 편집 판단

- 기존 `docs/structure-audit.md`와 확장성 JSON을 찾고 현재 코드·테스트·이전 감사에 대조했다. 기존 문서의 분류와 유용한 기록을 바탕으로 같은 파일을 개정했다.
- 기존 Supplier C diff와 89개 통과 기록은 과거 기록으로 구분했다. 새 adapter 전체 소스·테스트 diff·실행 XML이 해당 JSON에 없으므로 이번에 재현한 실험으로 표현하지 않았다.
- `docs/audit-evidence/structure-review.json`에 초기 문서의 hash, 정정 이유, import/테스트 목록, 이번 재현 소스와 실행 결과를 보존했다. 기존 JSON에는 재검토 안내를 추가했다.

### 이번에 직접 확인한 결과

- catalog client가 활성 설정에서 빠져도 감지하지 않았고, 중복 client 두 개는 한 동기화에서 저장 요청을 두 번 수행했다.
- A client가 B snapshot을 반환하면 B가 비활성이어도 store로 전달됐다. 실제 A/B 구현의 오작동이 아니라 잘못된 adapter에 대한 계약 방어 공백으로 분류했다. DB 오염을 직접 재현한 것은 아니다.
- search adapter의 `IllegalStateException`은 UNKNOWN/PARTIAL로 집계됐고 B의 유효 Offer 1개는 보존됐다.
- mapper 내부 NPE는 항목 거부로 집계됐지만 `IllegalStateException`은 전파됐다. C-01/C-02 입력 경계 변경과 함께 진단 분류를 보완하도록 제안했다.
- 실제 Boot/Tomcat의 예상 밖 500은 기본 JSON 형식이었고 주입한 내부 예외 메시지를 노출하지 않았다. 형식 불일치와 메시지 유출을 구분했다.

### 정정과 우선순위

- SRP/OCP/LSP/ISP/DIP를 모두 구체 코드와 연결했다. 구체 설정 의존, catalog writer의 저장 기술 결합, search domain의 Supplier enum 의존도 명시했다.
- B 본문 코드 매핑은 공통 HTTP/transport 매퍼가 아니라 B catalog/search client 두 곳에 있음을 바로잡았다.
- 정식 HTTP 테스트의 상대 socket 종료 보장을 철회하고, 해당 증거는 7.5-C 임시 검증에 한정했다.
- 카탈로그 일반 예외는 이미 WARN과 stack trace로 기록되며 정식 로그 테스트도 있었다. 관측성 부족을 로그 부재로 설명하지 않았다.
- 컨테이너 단일화·검증 유틸 통합·public 팩토리 검증 제거를 필수 작업으로 올리지 않았다. Testcontainers 공식 문서에서 실행 간 reuse와 CI 제약을 확인했다.
- 네 차례 감사의 8-A 정확성/계약, 8-B 자원/관측성, 8-C DB, 8-D 전체 연결 검증 순서를 정리하고 9단계 문서화·최종 요구사항 대조로 연결했다.

### 검증과 AI 활용

- 임시 경계 테스트 5개 + 실제 Boot API 테스트 1개: 6개 성공, 실패·오류·건너뜀 0개. 제품 문제를 재현하는 검증이며 해결을 뜻하지 않는다.
- 현재 정식 메인 테스트 89개, Mock 6개 및 설정 생성자 5개 파일·9곳을 재계수했다. 기존 전체 테스트와 Supplier C 실험은 반복하지 않았다.
- AI가 기존 문서의 사실·추론·권고를 구분하고 필요한 사례만 추가 재현했다. 제품 코드·설정·정식 테스트 수정, 커밋·push는 수행하지 않았다.

## 2026-09-16 - 감사 문서 공개 전 정리

- 감사 근거 JSON에 보관하던 이전 보고서 전문을 제거했다. 변경 추적에 필요한 hash와 정정 내역, 직접 작성한 측정·재현 소스는 유지했다.
- 서비스 요구사항 중심으로 표현을 정리하고 감사 항목 ID의 정의·통합 위치, 코드 줄 번호, Supplier 인증 헤더 대조 항목을 보완했다.
- 현재 파일과 로컬 Git 참조의 이력에서 제한된 이름, 개인 환경 경로, 알려진 비밀 값 형식과 직접 인용을 검사했다. 기존 이력에서 발견한 일반적인 자료 반입 금지 체크 문장은 비공개 원본 보관과 구분했다.
- JSON 구문·재현 소스 hash·문서 파일 링크를 확인했다. 제품 코드는 변경하지 않았으며 감사 결과와 구현 수정은 별도 커밋으로 관리한다.

## 2026-09-16 - 실행 안내와 진행 상태 정리

- 루트 README에 환경 준비, DB→Mock→메인 실행 순서, 검색 요청, 정상·부분·전체 실패 및 지연 Mock 명령을 정리했다.
- 모델에 포함하거나 제외한 정보와 이유, 품절 제외, 목록 동기화 시점, 자동 API 문서·추가 장애 제어 라이브러리의 보류 이유를 설명했다.
- 구현 계획의 미갱신 체크리스트를 기본 구현과 남은 검증을 구분하는 진행표로 바꿨다. 항목별 상세 근거는 감사 문서로 연결했다.
- Gradle의 두 실행 JAR 생성과 전체 테스트 작업을 offline dry-run으로 확인했다. Compose 설정 검증과 `--wait` 옵션, 문서 파일 링크도 확인했다.
- 이번 문서 작업에서 실제 DB·Mock·메인 프로세스를 함께 실행하거나 전체 테스트를 재실행하지 않았다. 전체 연결 검증은 8-D, 최종 실행 문서 확정은 9단계에 남긴다.
- AI는 현재 코드·설정과 실행 명령을 대조하고 문서를 작성했다. 제품 코드나 환경 파일은 변경하지 않았다.

## 2026-09-16 - 8-A 정확성·입력/예외 계약 보완

### 적용한 변경

- C-01/C-02: A/B 검색·카탈로그 어댑터에서 `JsonNode`의 실제 타입·정수 범위를 검사한다. 금액 소수 잘림, 숫자 문자열 변환을 막고 검색의 잘못된 항목만 격리한다. 카탈로그는 전체 snapshot을 거부한다.
- C-03: 정규화된 완전 중복을 제거하고 A의 일별 가격 순서를 정규화한다. 요금제 ID가 없어 금액·재고·조식·통화가 다른 Offer는 유지한다. 같은 내부 객실의 반환 가능한 Offer에서 수용 인원이 다르면 해당 객실의 Offer를 모두 제외한다.
- C-04: 숙소 코드 목록을 URI 템플릿 값으로 전달하여 특수문자를 보존한다. 쉼표 금지와 50개 분할은 유지한다.
- E-01/E-03: 명시적 항목 변환 실패와 내부 오류를 분리한다. 내부 오류는 `INTERNAL_ERROR`와 ERROR 로그로 식별하면서 정상 Supplier 결과를 유지한다. 미분류 HTTP/응답 읽기 오류는 `UNKNOWN`으로 남긴다.
- E-06: 카탈로그 client 중복·활성 client 누락을 시작 시 거부하고, 다른 Supplier의 snapshot은 저장하지 않는다. Publisher 생성 전 동기 예외도 제한 retry 정책을 따르게 했다.
- B 본문 오류 매핑을 작은 B 전용 매퍼로 합쳤다. HTTP 상태·B 본문 코드·timeout 원인 체인·고객 JSON 계약 테스트를 보완했다.
- 설계 문서에 입력·중복·충돌·오류 정책과 테스트 위치를 기록하고 README·구현 진행표를 갱신했다. 감사 문서 본문은 당시 관측으로 보존하고 상단에 후속 상태를 추가했다.

### 검증

- Java 21에서 compile과 집중 검증을 수행했다. 최초 집중 검증은 Docker daemon이 중단되어 Boot 통합 테스트 초기화에 실패했다. Docker Desktop 시작 후 동일 검증이 통과했다.
- 실제 Boot에 주입된 A/B WebClient를 사용하는 `SupplierInputBoundaryTests` 103개 사례가 통과했다. 소수·범위 초과·null·날짜·정상 형제 보존, 카탈로그 거부, 완전 중복·수용 인원 충돌·일별 순서, 특수 코드, B 본문 오류를 포함한다.
- `./gradlew test --rerun-tasks --offline --console=plain`으로 메인·Mock 전체 테스트를 실행했다. 이후 수용 인원 충돌과 API enum 검증을 추가하여 `./gradlew test --offline --console=plain`으로 메인 전체를 다시 실행했다. 변경 없는 Mock은 두 번째 실행에서 up-to-date였다.
- 최종 XML 집계: **메인 212개, Mock 6개 성공**, 실패·오류·건너뜀 0개. parameterized test는 입력별 실행 건수다. 기존 89개·6개는 감사 당시 기준점으로 유지한다.
- DB·별도 Mock 프로세스·고객 HTTP를 연결하는 E2E는 이번 검증 범위에 포함하지 않았다. 8-D에서 실행한다.

### 남은 작업과 AI 활용

- 다음은 8-B 전역 동시성·응답 크기·업무 지표, 이어서 8-C DB 동시 실행/시간 예산, 8-D 전체 연결 검증, 9단계 최종 문서화다.
- 공통 JSON 타입 검사는 어댑터 내부 입력 처리에 한정하고, 범용 client registry나 전체 모델 재설계는 도입하지 않았다. 내부 오류 분류는 공개 enum에 추가되므로 API 테스트와 문서를 함께 갱신했다.
- AI가 코드·테스트·문서를 수정하고 로컬 전체 검증을 실행했다. 이번 변경은 아직 커밋하거나 push하지 않았다.

## 2026-09-16 - 8-A 리뷰 보완: 입력 오류와 내부 오류 경계

- 카탈로그뿐 아니라 검색 항목 변환도 일반 IllegalArgumentException을 포괄 변환하고 있음을 확인했다. JSON 검사와 외부 값의 도메인 검증에 InvalidValueException을 사용하고, 일반 내부 예외는 전파하도록 경계를 좁혔다. 금액 overflow는 정확한 덧셈 지점에서 변환한다. 내부 ID와 계산 결과의 불변식 오류는 일반 예외로 유지한다.
- A/B 실제 어댑터 경로에 일반 IllegalArgumentException을 주입하여 검색과 카탈로그 모두 원래 오류를 전파하는 회귀 테스트 2개를 추가했다. 기존 외부 입력 거부 사례도 유지했다.
- SupplierInputBoundaryTests를 SupplierAdapterContractTests로 이름을 맞추고 하나의 실제 Boot 환경 안에서 입력·Offer 정책·URI·본문 오류를 Nested 그룹으로 나눴다. 본문 오류는 HTTP 어댑터 검증이므로 순수 매퍼 테스트로 옮기지 않았다. 새 테스트의 압축 포맷을 정리했다.
- 카탈로그의 반복 거부가 기존 매핑 노후화로 이어지는 절충과 숙소 단위 배치가 충돌 검사의 전제임을 설계에 기록했다. 중복 관측, 카탈로그 신선도, 용량 부족 분류는 8-B에서 함께 다룬다.
- Java 21에서 전체 테스트 실행 성공: 메인 214개, Mock 6개, 실패·오류·건너뜀 0개. 최초 집중 실행은 메인 테스트가 통과했으나 Mock 모듈에 해당 이름이 없어 Gradle 필터에서 실패했다. 필터 없는 전체 실행으로 확인했다.
- AI가 코드·문서·회귀 테스트를 수정하고 검증했다. 포맷 도구는 임시 경로에서만 사용했고 프로젝트 의존성에는 추가하지 않았다.


## 2026-09-16 - 8-B 자원 제한·관측성

### 변경과 결정

- Supplier·작업별 JVM 허용량을 검색 8개·카탈로그 1개로 추가했다. 구독 시 즉시 수용/거부하고 성공·실패·취소에 반환한다. 검색과 카탈로그를 같은 호스트에 연결해도 독립된 풀을 사용하며 연결 수·pending 개수·200ms 획득 대기를 명시했다.
- codec 한도는 검색 2 MiB·카탈로그 8 MiB로 유한하게 설정했다. CAPACITY_EXCEEDED/RESPONSE_TOO_LARGE를 내부 버그와 구분하며 두 유형은 즉시 retry하지 않는다. transport 계층 자동 재전송은 끄고 카탈로그의 기존 제한 retry만 유지했다.
- 호출·검색 최종 결과·구간 시간·중복/거부 Offer·카탈로그 저장 성공 시각과 연속 실패 지표를 추가했다. HTTP 200 본문 오류를 업무 실패로 기록한다. 중복은 별도 건수로만 관측하고 고객 API에 새 건수 필드는 추가하지 않았다.
- 로그와 메타데이터 조인에만 쓰던 임시 Offer 목록을 제거했다. 숙소 단위 분할, 반환 정렬, 일별 금액 근거는 유지했다.
- JVM 자원 한도가 고객 진입 제어·DB 저장 직렬화·전체 heap 상한을 보장하지 않는다는 점을 설계 §17에 명시했다. 8-C DB와 8-D 전체 연결 작업은 수행하지 않았다.

### 검증과 시행착오

- 실제 HTTP 검증 7개: 공유 서비스의 동시 고객 한도·정상 Supplier 보존·별도 카탈로그 자원, 전체 timeout 소켓 종료와 회복, 본문 오류 지표, 정상 큰 응답, 한도 초과와 회복, 실제 풀 pending/획득 timeout, 중복 지표.
- 첫 HTTP 검증은 테스트 서버의 path 비교가 요청 URI와 다르게 정규화되어 fixture 조회가 실패했다. URI에서 경로를 직접 추출하도록 테스트 서버를 수정한 뒤 통과했다. 제품 장애로 기록하지 않는다.
- 실제 Boot codec 검증 3개를 추가했다. 3,000개 숙소 카탈로그와 50개 숙소·5개 객실·30박 검색을 수용하며 A/B의 검색·카탈로그 한도 초과는 전용 실패로 분류됐다.
- 허용량 생명주기·설정 테스트 3개와 저장 후 신선도 테스트 1개를 추가했다. 허용량 1개에서 즉시 재구독 100회를 처리하고, 동기 예외·취소 후 회복을 확인했다.
- 새 실패 유형 두 개의 부분/전체 실패 HTTP 응답 계약 4개 사례를 추가했다.
- 최종 `./gradlew test --offline --console=plain` 검증: 메인 232개·Mock 6개, 실패·오류·건너뜀 0개. 변경 없는 Mock은 최종 실행에서는 up-to-date이며 앞선 리뷰 보완 전체 실행에서 실제 성공했다.
- AI가 코드·테스트·문서를 작성하고 현재 Reactor Netty/Spring 공식 문서와 로컬 의존성을 확인했다. 설정값의 단순 버퍼 예산 48 MiB를 peak heap 실측으로 표현하지 않았다.
