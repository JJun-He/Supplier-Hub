# AI 활용과 검증 기록

AI를 설계 대안 비교, 제품 코드·테스트 작성, 결함 재현, 리뷰와 문서 정리에 활용했다. 구현과 실행 검증에도 상당 부분 참여했다. 사용자는 진행 범위를 결정하고 추가 검토를 요청했으며, AI는 제안의 근거를 비교하고 개발 도구로 구현·검증했다. 사용자가 모든 코드를 수작업으로 작성하거나 모든 검증을 따로 재수행했다는 뜻은 아니다.

이 문서는 제안을 채택하거나 바꾼 이유를 정리한다. 날짜별 변경과 실제 커밋은 [개발 기록](JOURNAL.md), 현재 계약은 [설계 문서](docs/architecture-decisions.md)에 있다.

## 설계 제안의 수용·수정·보류

| 주제 | 검토와 판단 | 반영한 결과 |
| --- | --- | --- |
| 내부 ID | UUID를 먼저 검토했지만 분산 발급 요구가 없고 하나의 관계형 DB가 식별자를 관리한다. | **수정:** `BIGINT IDENTITY`를 사용하고 비활성 매핑을 보존해 재등장 시 같은 ID를 쓴다. |
| Offer와 가격 | 판매 조건을 객실 타입과 분리하고 Supplier별 가격 의미를 하나로 맞출 공통 모델이 필요하다. | **수용:** Offer를 판매 단위로 두고 가격은 세금 포함 숙박 전체 총액으로 통일했다. 없는 일별 가격은 역산하지 않는다. |
| 카탈로그 갱신 | 기동 시 한 번만 실행하면 최초 장애 복구 후에도 상품을 확보하지 못한다. HTTP 성공 한 번으로 목록 완전성을 확정하기도 어렵다. | **수정:** 기동 직후·주기 실행, 2회 연속 누락 확인과 급감 snapshot 거부를 적용했다. 거부가 반복되면 매핑이 오래 유지되는 절충도 기록했다. |
| 메시징·스트리밍·배치 | Supplier는 날짜·인원을 받는 요청/응답 계약이고 검색의 50개 분할은 영속 배치 작업이 아니다. | **보류:** WebSocket·MQ·Kafka·Spring Batch를 추가하지 않았다. MVC·JPA와 제한 병렬 `WebClient`를 사용한다. |
| 검색 결과 조립 | 전체 카탈로그를 결과 객체에 복사하고 API에서 Offer와 조인할 필요가 있는지 재검토했다. | **수정:** 반환된 Offer에 필요한 이름만 application의 `SearchOffer`에 결합한다. 전체 projection 조회·순회는 남으며 처리 시간·GC 개선율은 주장하지 않는다. |
| 구조 공통화 | writer의 패키지 이동, 범용 client registry, 설정 Map 전환 등을 검토했다. 이동만으로 정책과 저장 구현이 분리되지는 않으며 기존 switch·시작 검증은 안전장치다. | **보류·철회:** 명확한 이점 없는 구조 변경을 제외하고 실제 계약 방어 공백을 우선했다. |

초기 정책은 [설계 커밋 `6ab923e`](https://github.com/JJun-He/Supplier-Hub/commit/6ab923e), 결과 조립 변경은 [`eff5bb8`](https://github.com/JJun-He/Supplier-Hub/commit/eff5bb8), 구조 판단의 정정은 [구조 감사](docs/archive/structure-audit.md)에 남아 있다.

## 재현과 리뷰로 바꾼 구현

AI가 작성한 코드·테스트와 설명도 다시 검토 대상으로 삼았다. 통과한 테스트 수만으로 계약 충족을 판단하지 않고 실제 설정에서 문제가 재현되는지, 단언이 무엇을 보장하는지 확인했다.

| 사례 | 발견한 문제 또는 과도한 주장 | 수용·수정한 내용과 근거 |
| --- | --- | --- |
| 금액·항목 파싱 | 실제 Boot codec에서 음수 소수가 0으로 변환됐고, 한 항목의 날짜·타입 오류가 정상 형제 결과까지 버렸다. | JSON 실제 타입·정수 범위를 검사하고 검색 항목별 변환을 격리했다. [어댑터 계약 테스트](src/test/java/com/supplierhub/supplier/common/SupplierAdapterContractTests.java)에 정식 회귀 사례를 추가했다. |
| 외부 입력과 내부 오류 | 일반 `IllegalArgumentException`까지 외부 입력 오류로 바꾸면 내부 결함이 잘못된 Supplier 데이터로 숨는다. | 명시적 입력 검증의 `InvalidValueException`과 내부 예외를 구분했다. 어댑터에 내부 예외를 주입해 원래 오류가 전파되는지 검증했다([`33611ab`](https://github.com/JJun-He/Supplier-Hub/commit/33611ab)). |
| JVM 전체 호출량 | 요청당 4개 제한을 지켜도 고객 8명의 동시 검색에서는 Supplier별 32개까지 호출됐다. | Supplier·작업별 JVM 허용량과 별도 풀을 추가하고 성공·실패·취소의 반환, 거부 후 회복을 실제 HTTP로 확인했다. 8개 초기값을 실측 처리량으로 표현하지 않았다([`1176296`](https://github.com/JJun-He/Supplier-Hub/commit/1176296)). |
| 응답 크기·업무 지표 | 정상 큰 응답이 기본 버퍼를 넘었고 B의 HTTP 200 본문 오류는 HTTP 지표에서 성공으로 보였다. | 검색·카탈로그의 유한한 버퍼와 전용 실패 유형, 업무 결과 지표를 추가했다. 버퍼 합계와 peak heap 실측은 구분했다. |
| DB 시간 예산 | 검색의 reactive 제한 시간만으로는 DB 잠금·연결 대기를 다루지 못했다. | 연결 획득 시간을 차감한 transaction-local SQL·잠금 제한과 늦은 결과 거부를 구현했다. 실제 PostgreSQL 잠금·쿼리 취소·풀 고갈·설정 복구를 검증했다([`86ca690`](https://github.com/JJun-He/Supplier-Hub/commit/86ca690)). |
| 동기화 guard와 SQL 설명 | 운영 스케줄은 이미 직렬화돼 있었고 DB 예산의 설정 SQL은 Hibernate projection 통계에 포함되지 않는다. | guard를 추가 서비스 호출의 중복 방어로 한정했다. 설정 SELECT 1개와 데이터 SELECT 1개를 구분하고 다중 인스턴스 지원·추가 왕복 성능 개선 주장을 제외했다([`6231c8a`](https://github.com/JJun-He/Supplier-Hub/commit/6231c8a)). |
| E2E 호출 지표 | `supplier.calls`는 논리 호출 수여서 실제 HTTP 재전송 횟수까지 증명하지 않는다. 완료 지표가 늦게 기록되면 다음 요청의 기준값과 겹칠 수도 있다. | 과도한 테스트 이름·설명을 수정하고 매 검색의 지표 완료를 기다린다. [E2E 테스트](src/e2eTest/java/com/supplierhub/e2e/SupplierHubEndToEndTests.java)에 반영했다. |
| E2E 검증·정리 | 카탈로그 행을 Map에 덮어쓰면 잘못된 중복을 놓치고, 테스트 JVM이 먼저 종료되면 자식 서버가 남을 수 있다. | Supplier·숙소·객실 코드와 중복 행을 검증하고 JVM 종료 훅에서 자식 서버를 정리했다([`2478723`](https://github.com/JJun-He/Supplier-Hub/commit/2478723)). |

감사는 문제 재현과 개선 제안을 분리했다. 추가 인덱스의 일관된 개선은 확인하지 못해 도입하지 않았고, batching 실험 결과를 운영 성능 향상으로 확대하지 않았다. 기존 Supplier C 확장 실험은 전체 재현 자료가 없어 재검토에서 반복한 실험으로 표현하지 않았다. 당시 조건과 한계는 [감사 보관 자료](docs/archive/)에 남겼다.

2026-09-17 후속 리뷰에서는 타임아웃 값의 선택 이유가 부족하다는 지적을 수용했다. 앞선 AI 검토도 이 설명 누락을 놓쳤다. 다만 제안에 포함된 고객 응답 목표 3초, 같은 리전의 연결 지연, 파싱에 예약된 1초, 사용자 이탈 임계는 측정·합의 근거가 없어 채택하지 않았다. 현재 설정을 유지할 정책적 이유와 각 타이머의 실제 적용 범위를 [설계 문서](docs/architecture-decisions.md#call-resources)에 보완했다. 서킷 브레이커도 잠정 수치와 검증 조건을 갖춘 [제안 설계](docs/architecture-decisions.md#circuit-breaker-design)로만 기록하고 구현·검증 완료로 표현하지 않았다.

## 후속 리뷰에서 수용한 수정

2026-09-17 추가 리뷰에서는 카탈로그 본문 수신 중 timeout의 오분류를 실제 HTTP로 재현했고, 사용자가 타임아웃·준비 상태·실패 로그 개선과 후속 정리를 진행하도록 결정했다. AI는 아래 범위를 구현하고 독립 검토·실행 검증했다.

- **수용:** 응답 예외의 중첩 timeout을 공통 매퍼에서 분류한다. 전체 검색 재시도는 추가하지 않는다.
- **수정:** 정상 빈 카탈로그 구별에 메모리 지표를 재사용하면 재시작 후 의미가 사라진다. 최초 반영 여부만 DB에 저장하고 매핑과 원자적으로 commit하며, 두 조회의 일관성과 잔여 예산도 함께 검증했다.
- **수용:** 항목 오류에 검색·배치·외부 키 문맥을 붙이고 배치당 상세 5건과 집계로 제한했다. 처리량 향상 수치는 주장하지 않는다.
- **보류:** 대량 누락의 반복 수신만으로 자동 승인하는 정책, 강제 반영 진입점, 공개 Offer 중복 정책 변경과 광범위한 구조 재편은 포함하지 않았다.

후속 제안에서는 공개 Offer 중복 제거와 대량 삭제 승인 구현을 다음 작업으로 권했지만, 과제 범위와 현재 계약을 다시 대조해 철회했다. 현재 반환 건수는 이미 응답과 일치하며, 공개 중복 제거는 상품 동일성과 대표 가격 근거를 정하는 정책 변경이다. 관리자 승인 기능도 비범위이므로 구현을 추가하지 않고 중복 보존 이유와 기존 복구 제약을 문서에 남겼다.

타임아웃, 로그, 준비 상태 E2E를 파일 소유 범위로 분담하고 총괄이 DB·검색 연결과 테스트 통합을 맡았다. 독립 리뷰에서 두 번째 SQL 조회에도 남은 예산을 다시 적용할 필요를 찾아 반영했다. 최종 전체 실행은 메인 302개·Mock 6개·E2E 12개, 합계 320개가 모두 실제 실행돼 실패·오류·건너뜀 0개였다. 상세 조건은 [실행 검증](docs/e2e-verification.md#2026-09-17-타임아웃준비-상태진단-보완-후-검증)을 따른다.

## 작업 분담과 실행 근거

전체 연결 검증과 주석 정리에서는 총괄 1개·보조 3개의 에이전트가 구현, 기대값·단언 리뷰, 실행 환경·Gradle·프로세스 정리를 나누었다. 모든 개발 작업이 항상 같은 구성으로 진행된 것은 아니다. 담당 수정 파일을 구분하고 리뷰에는 읽기 전용 범위를 주었으며, 테스트 실행과 최종 통합은 총괄에서 수행했다.

역할을 나눈 AI 리뷰도 같은 전제를 놓칠 수 있다. 그래서 제안의 합의와 실행 결과를 구분하고, 실제 Boot 주입 어댑터·PostgreSQL·고객 HTTP로 검증 범위를 넓혔다. 현재도 부하·GC·다중 인스턴스·DNS/TLS 장애 등 검증하지 않은 조건은 성공으로 기록하지 않는다.

2026-09-16 전체 연결 검증 작업에서 메인 270개와 E2E 9개를 실제 실행했다. 변경 없는 Mock 6개는 기존 성공 결과를 재사용했다. 최종 `check` 호출에서는 E2E 9개를 재실행하고 메인·Mock은 up-to-date 결과를 재사용해 합계 285개, 실패·오류·건너뜀 0개를 확인했다. 같은 날의 실행 순서와 최신 문서 정리 검증은 [개발 기록](JOURNAL.md), E2E 시나리오와 한계는 [전체 연결 검증](docs/e2e-verification.md)에 구분한다.

2026-09-17 문서 정리에서는 총괄이 빌드 결과 없는 임시 소스에서 메인 270개·Mock 6개·E2E 9개를 모두 실제 재실행했다. 합계 285개, 실패·오류·건너뜀 0개이며 의존성과 컨테이너 이미지는 로컬 캐시를 사용했다. 새 Compose DB와 고정 포트 `bootRun`·`curl`도 재현했다. 보조 작업은 현재 설계·요구사항 대조, README 검토, 개발·AI 기록 정리로 나누었다. 제품 코드·테스트는 변경하지 않았으며 실행 조건과 한계는 [최종 재현 기록](docs/e2e-verification.md#2026-09-17-최종-재현)에 남겼다.

## 컨텍스트와 실행 비용 관리

| 적용한 방식 | 목적과 한계 |
| --- | --- |
| 파일·검색어·줄 범위를 지정하고 도구 출력에 상한을 둠 | 관련 코드와 결과를 중심으로 확인한다. 긴 로그는 파일로 남겨 필요한 부분을 조회했다. |
| 구현 파일과 검토 질문을 정해 분담 | 같은 파일의 중복 수정과 검토 범위 확산을 줄이기 위한 조치다. |
| 보조 작업을 모은 뒤 총괄에서 검증 | 각 에이전트가 같은 테스트를 반복 실행하는 일을 줄였다. |
| 결정·실행 근거를 저장소 문서에 기록 | 후속 작업에서 이전 판단과 검증 범위를 확인한다. `AGENTS.md`는 구현·전체 연결 검증 이후 문서 정리부터 적용했다. |
| 변경 범위에 맞는 검증과 Gradle 성공 결과 재사용 | 문서만 바뀐 경우 링크·코드 대조를 하고, 실행한 테스트와 재사용한 결과를 구분했다. |

LLM 입력·출력·캐시 토큰과 작업별 비용을 수집하지 않았으므로 토큰 절감률·비용 개선 수치는 제시하지 않는다. 다중 에이전트는 공유 문맥의 중복 처리와 조율 메시지로 총 사용량을 늘릴 수 있으며 전체 대화 문맥을 전달한 경우도 있다. 단일 에이전트 대비 시간·비용, 모델별 비용 배분이나 캐시 최적화의 효과는 측정하지 않았다. 테스트 재실행을 줄인 것은 실행 비용 관리이며 LLM 토큰 절감 측정과는 별개다.
