# AI 활용과 검증 기록

이 프로젝트에서는 AI를 설계 대안 비교, 코드·테스트 작성, 결함 재현, 리뷰와 문서 정리에 활용했다. 구현과 검증에도 상당 부분 참여했으며, 제안의 채택 여부와 실행 근거를 [JOURNAL](JOURNAL.md)에 단계별로 기록했다. 이 문서는 그 활용 방식과 한계를 요약한다.

2026-09-16, 8-D 완료 시점 기준이다. 최종 수동 실행 재현과 요구사항·문서 대조는 9단계에 남아 있다.

## 1. 작업 범위와 역할

사용자는 단계별 진행을 결정하고, 추가 리뷰를 전달해 타당성 검토와 보완을 요청했다. AI는 설계 대안 제시부터 구현·테스트 실행·문서 작성까지 수행했다. 아래 테스트 실행 결과는 AI가 개발 도구로 실행한 결과이며, 사용자가 모든 코드를 수작업으로 작성하거나 모든 검증을 별도로 재수행했다는 의미는 아니다.

8-D와 주석 정리에서는 **총괄 에이전트 1개와 보조 에이전트 3개**가 역할을 분담했다. 이는 해당 작업의 구성으로, 전체 개발 과정에서 항상 같은 수를 동시에 실행한 것은 아니다.

| 역할 | 수행 내용 |
| --- | --- |
| 총괄·통합 | 범위 설정, Gradle 연결, 변경 통합, 실제 테스트 실행, 결과 문서와 커밋 정리 |
| E2E 구현 | PostgreSQL·메인·Mock의 별도 프로세스 연결, 고객 HTTP 시나리오와 검증 코드 작성 |
| 기대값·검증 범위 검토 | Mock 데이터에서 금액·재고 기대값 계산, 테스트 단언과 계측 지표의 의미 검토 |
| 주석·실행 환경 검토 | 영어 설명 주석을 한국어로 정리, 실행 환경 격리·프로세스 정리·Gradle 입력 추적 검토 |

보조 에이전트의 수정 파일을 구분하고, 리뷰에는 읽기 전용 범위를 부여했다. 테스트 실행과 최종 통합은 총괄에서 수행했다. 역할을 나눈 AI 리뷰도 공통된 전제를 놓칠 수 있으므로, 합의 여부와 별개로 실행 결과를 확인했다.

## 2. 검토를 통해 바뀐 내용

| 검토 대상 | 발견한 문제 또는 한계 | 반영한 내용 |
| --- | --- | --- |
| E2E 호출 횟수 | `supplier.calls`는 계측 경계의 논리 호출 수여서 실제 HTTP 재전송 횟수를 증명하지 않음 | `WithoutRetry` 테스트명을 수정하고 검증 범위를 문서화 |
| 지표 완료 시점 | 이전 검색의 완료 지표가 늦게 기록되면 다음 검색의 기준값과 겹칠 수 있음 | 검색마다 완료 지표 증가를 확인한 뒤 다음 요청 진행 |
| 카탈로그 매핑 | 숙소 코드로 Map에 덮어쓰면 잘못된 객실 중복을 놓칠 수 있음 | 중복 행 거부와 Supplier·숙소·객실 코드 대조 추가 |
| 테스트 프로세스 종료 | 테스트 JVM 종료 시 JUnit 종료 처리가 생략되면 자식 서버가 남을 수 있음 | JVM 종료 훅에 자식 서버 종료 등록 |
| 8-C 동기화 보호 설명 | 현재 운영 경로의 직렬화는 기존 `fixedDelay`가 제공함 | 추가 guard의 역할을 향후 서비스 호출에 대한 보조 방어로 한정 |

E2E 관련 보완은 [테스트 코드](src/e2eTest/java/com/supplierhub/e2e/SupplierHubEndToEndTests.java), [프로세스 실행 코드](src/e2eTest/java/com/supplierhub/e2e/JarProcess.java), [검증 결과](docs/e2e-verification.md)에 남아 있다. 8-C 설명 보완의 근거는 [설계 §18](docs/architecture-decisions.md#18-8-c-동기화-중복-실행과-db-시간-예산)에 있다.

## 3. 컨텍스트·토큰·실행 비용 관리

이 절은 개발 중 AI 작업의 효율화 기록이다. LLM 입력·출력 토큰, 캐시 사용량, 비용을 작업별로 수집하지 않았으므로 **토큰 절감률이나 비용 개선 수치는 제시하지 않는다.**

| 실제 적용한 방식 | 목적과 한계 |
| --- | --- |
| 검색어·파일·줄 범위를 지정한 조회와 도구 출력 상한 | 관련 코드와 결과를 중심으로 확인. 긴 로그는 파일에 보존하고 필요한 부분을 조회 |
| 구현 파일과 검토 질문을 지정한 작업 분담 | 동일 파일의 중복 수정과 검토 범위 확산을 줄이기 위한 조치 |
| 보조 에이전트의 결과 보고 후 총괄에서 테스트 실행 | 같은 검증을 각 에이전트가 반복 실행하는 일을 줄임 |
| 결정·실행 결과를 저장소 문서에 기록 | 후속 작업에서 이전 결정과 검증 근거를 다시 찾을 수 있게 함 |
| 변경 범위에 맞는 검증과 Gradle의 성공 결과 재사용 | 변경 없는 테스트의 재실행을 줄임. 실행 비용 관리이며 LLM 토큰 절감량과는 별개 |

다중 에이전트는 공유 문맥의 중복 처리와 조율 메시지로 총 토큰 사용량이 늘 수 있다. 전체 대화 문맥을 전달한 경우도 있다. 이번 작업의 목적은 구현과 검토의 분담이며, 단일 에이전트 대비 시간·비용이나 모델별 비용 배분·캐시 최적화의 효과는 별도로 측정하지 않았다.

후속 측정에서는 작업별 입력·출력·캐시 토큰, 실행 시간, 재작업 횟수를 함께 기록할 수 있다. 같은 범위와 검증 조건에서 결과 품질을 확인해야 하며, 현재 문서에는 이를 이미 수행한 최적화로 기재하지 않는다.

## 4. 실행 근거와 남은 확인

8-D 작업에서 메인 테스트 270개와 E2E 9개를 실제 실행했다. 변경 없는 Mock 6개는 기존 성공 결과를 재사용했다. 최종 결과는 합계 285개, 실패·오류·건너뜀 0개다. E2E는 실제 PostgreSQL과 별도 JVM의 메인·Mock을 연결하고, 고객 HTTP 응답과 DB 상태를 대조했다.

영어 설명 주석 변경은 Java 문자열을 보존하는 토큰 비교로 실행 코드가 달라지지 않았는지 확인했다. 이때의 Java 토큰 비교는 LLM 사용량 측정과는 관계없다.

| 변경 | 커밋 |
| --- | --- |
| 8-C 설명·비용·포트 계약 보완 | `6231c8a` |
| 코드 주석 한국어 정리 | `076256c` |
| 8-D E2E 구현·검증 | `2478723` |

9단계에서는 고정 포트 수동 실행 재현과 요구사항→구현→테스트→문서 대조를 진행한다. 카탈로그 동기화부터 검색 응답까지의 흐름, 부분 실패와 DB 시간 예산의 한계는 설명과 변경 검증이 필요한 핵심 경로다. 현재까지의 실행 범위와 미검증 조건은 [전체 연결 검증 문서](docs/e2e-verification.md)에 구분했다.
